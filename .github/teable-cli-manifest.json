{
  "name": "teable",
  "version": "0.6.30",
  "description": "Teable AI Tools CLI for operating Teable bases, tables, fields, views, records, SQL read queries, and related app/automation workflows with a safe read-before-write process.",
  "options": [
    {
      "flags": "--debug",
      "description": "Enable debug output (verbose logging to stderr)"
    },
    {
      "flags": "-V, --version",
      "description": "Show CLI version"
    }
  ],
  "help": "Usage: teable [options] [command]\n\nTeable AI Tools CLI for operating Teable bases, tables, fields, views, records, SQL read queries,\nand related app/automation workflows with a safe read-before-write process.\n\nOptions:\n  -V, --version                Show CLI version\n  --debug                      Enable debug output (verbose logging to stderr)\n  -h, --help                   display help for command\n\nSetup:\n  auth|init [options]          Manage auth for teable\n  config                       Manage project-level CLI configuration\n\nResources:\n  base                         base operations (create, get, list, update, delete)\n  table                        table operations (create, get, update, delete)\n  field                        field operations (create, get, update, delete)\n  record                       record operations (create, get, update, delete)\n  view                         view operations (create, get, update, delete)\n  app                          app operations (list, get-code, create, update, delete, publish,\n                               status, unpublish, login-config, ai-docs, ai-enable)\n  automation                   automation operations (list, get, update, activate, setup-trigger,\n                               delete, test-node, delete-node, generate-script, generate-flowchart,\n                               get-script-input, get-runs, get-run)\n  authority                    authority operations (get, export, diff, apply, enable, disable,\n                               role-list, role-get, role-delete, role-duplicate)\n  folder                       folder operations (create, update, delete, move)\n  integration                  integration operations (connect, list, get-token)\n  env                          env operations (list, set, update, delete)\n  sandbox                      sandbox operations (tool)\n  skill                        skill operations (list, import-github, import-file, update)\n\nTools:\n  get-node-tree [options]      Get the tree structure of all nodes (tables, automations, apps, etc.)\n                               in a base\n  search-api [options]         Search available APIs by description to find the right endpoint\n  get-doc [options]            Get documentation for a topic\n  tools                        Tool discovery and metadata commands\n  sql-query [options]          Execute SQL query on tables\n  get-collaborators [options]  Get collaborators in the current base with pagination and search\n  get-ai-config [options]      Get AI field configuration documentation with available models\n  import [options]             Import tabular data into Teable (analyzes by default; --table-name\n                               creates, --table-id appends)\n  import-airtable [options]    Import an Airtable base into Teable\n  import-status [options]      Query import job status for a table\n  upload-attachment [options]  Upload a local file as an attachment\n  call-api [options]           Call any Teable API (axios-style: method, url, params, data)\n  trigger-ai-fill [options]    Trigger AI generation for an AI-configured field\n  scrape [options]             Scrape structured data from URLs — supports LinkedIn, Amazon,\n                               Instagram, TikTok, YouTube, X/Twitter, Reddit, and 35+ more platforms\n  send-email [options]         Send an email to one or more recipients (system SMTP by default, or\n                               --smtp for a custom transport)\n\nCommands:\n  help [command]               display help for command\n\nExamples:\n  teable table get --table-id tblXXXX            List table metadata\n  teable record get --table-id tblXXXX           Query records\n  teable table create --name Tasks --fields '[\"Title:text\",\"Status:sel:Todo,Done\"]'\n",
  "commands": [
    {
      "name": "app",
      "description": "app operations (list, get-code, create, update, delete, publish, status, unpublish, login-config, ai-docs, ai-enable)",
      "subcommands": [
        {
          "name": "ai-docs",
          "summary": "How the app calls AI through Teable proxy (usage + available model keys)",
          "description": "Shortcut for: get-doc --topic app.ai\n\nReturns documentation on how an app-builder app calls AI through Teable's\nproxy, with the available model keys for the current base. Covers:\n  - Injected env vars (TEABLE_AI_API_BASE_URL / TEABLE_AI_API_KEY)\n  - Server-side usage with the OpenAI / Anthropic SDK\n  - How to enable AI access if it is off\n  - Available model keys (e.g. aiGateway@openai/gpt-5.5@teable)",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app ai-docs [options]\n\nShortcut for: get-doc --topic app.ai\n\nReturns documentation on how an app-builder app calls AI through Teable's\nproxy, with the available model keys for the current base. Covers:\n  - Injected env vars (TEABLE_AI_API_BASE_URL / TEABLE_AI_API_KEY)\n  - Server-side usage with the OpenAI / Anthropic SDK\n  - How to enable AI access if it is off\n  - Available model keys (e.g. aiGateway@openai/gpt-5.5@teable)\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "ai-enable",
          "summary": "Enable AI access for an app (provisions the AI proxy key)",
          "description": "Enable AI access for an app so it can call AI through Teable's proxy.\nProvisions the proxy key and injects TEABLE_AI_API_BASE_URL /\nTEABLE_AI_API_KEY on the next preview restart. Idempotent — safe to run\nwhen already enabled. System-model usage consumes credits; BYOK models\nrun on the space's own key (no Teable credits).\n\nThe key value is never printed. Run `app ai-docs` for usage + models.",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App id (appXXXX); defaults to TEABLE_APP_ID"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app ai-enable [options]\n\nEnable AI access for an app so it can call AI through Teable's proxy.\nProvisions the proxy key and injects TEABLE_AI_API_BASE_URL /\nTEABLE_AI_API_KEY on the next preview restart. Idempotent — safe to run\nwhen already enabled. System-model usage consumes credits; BYOK models\nrun on the space's own key (no Teable credits).\n\nThe key value is never printed. Run `app ai-docs` for usage + models.\n\nOptions:\n  --app-id <appId>        App id (appXXXX); defaults to TEABLE_APP_ID\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "create",
          "summary": "Create or update an app with AI",
          "description": "Build or modify a web application with Teable integration.\n\nActions:\n  create: Create a new app\n  update: Modify an existing app\n\nParameters:\n  action: create or update (required)\n  name: App name (optional)\n  prompt: App-building prompt for teable app create/update CLI (required)\n  appId: App ID to update (required for update action)\n  tableIds: Table IDs to include in app context (optional)\n  attachmentTokens: Image tokens for UI reference (optional)\n\nImportant rules for prompt:\n  First complete any requested setup, table inspection, analysis, or data changes.\n  Do not pass setup, analysis, or data-seeding steps to App Builder.\n  Include only the app purpose, UI/workflow, explicit constraints, relevant table IDs, and verified data context.\n  Keep the user intent without adding extra features or details.\n  Do not use markdown formatting in the prompt.\n  Do not specify technology stack unless the user explicitly requests it.\n\nWorkflow:\n  Use get-apps tool first to check existing apps.\n  If a matching app exists, use update action.\n  Otherwise, use create action.",
          "options": [
            {
              "flags": "--attachment-tokens <jsonArray>",
              "description": "Optional array of attachment tokens (screenshots, designs, etc.)"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "Folder to place the new app in (`folderId` from Active Tab Meta); omit for base root."
            },
            {
              "flags": "--name <name>",
              "description": "Name for the app"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--prompt <prompt>",
              "description": "App-building prompt. Include only app goals, constraints, and verified data context.",
              "required": true
            },
            {
              "flags": "--table-ids <jsonArray>",
              "description": "Optional array of table IDs to include in the app context"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app create [options]\n\nBuild or modify a web application with Teable integration.\n\nActions:\n  create: Create a new app\n  update: Modify an existing app\n\nParameters:\n  action: create or update (required)\n  name: App name (optional)\n  prompt: App-building prompt for teable app create/update CLI (required)\n  appId: App ID to update (required for update action)\n  tableIds: Table IDs to include in app context (optional)\n  attachmentTokens: Image tokens for UI reference (optional)\n\nImportant rules for prompt:\n  First complete any requested setup, table inspection, analysis, or data changes.\n  Do not pass setup, analysis, or data-seeding steps to App Builder.\n  Include only the app purpose, UI/workflow, explicit constraints, relevant table IDs, and verified\ndata context.\n  Keep the user intent without adding extra features or details.\n  Do not use markdown formatting in the prompt.\n  Do not specify technology stack unless the user explicitly requests it.\n\nWorkflow:\n  Use get-apps tool first to check existing apps.\n  If a matching app exists, use update action.\n  Otherwise, use create action.\n\nOptions:\n  --prompt <prompt>                App-building prompt. Include only app goals, constraints, and\n                                   verified data context.\n  --name <name>                    Name for the app\n  --table-ids <jsonArray>          Optional array of table IDs to include in the app context\n  --attachment-tokens <jsonArray>  Optional array of attachment tokens (screenshots, designs, etc.)\n  --folder-id <folderId>           Folder to place the new app in (`folderId` from Active Tab Meta);\n                                   omit for base root.\n  -b, --base-id <baseId>           Target base ID (required, e.g. bseXXXX). Set default: `teable\n                                   config base-id <id>`.\n  --token <token>                  Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>            Teable API server base URL\n  --pretty                         Indent JSON for human readability (default: false)\n  -h, --help                       display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete an app",
          "description": "Delete an app (moves it to trash).\n\nParameters:\n  - appId (required): The ID of the app to delete\n\n⚠️ Warning: This operation is destructive. Verify appId before deletion. The app can be restored from the base trash.",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "The ID of the app to delete",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app delete [options]\n\nDelete an app (moves it to trash).\n\nParameters:\n  - appId (required): The ID of the app to delete\n\n⚠️ Warning: This operation is destructive. Verify appId before deletion. The app can be restored\nfrom the base trash.\n\nOptions:\n  --app-id <appId>        The ID of the app to delete\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "get-code",
          "summary": "Download app source code as a local read-only context snapshot",
          "description": "Download the source code of an app into the local workspace for reading.\n\nUse this when the user mentions an app (an `@app[name](id:appXXX)` marker) and you need its\nimplementation as context: understanding how it works, referencing or imitating it, or\ncoordinating changes across apps.\n\nParameters:\n  - appId (required): The app ID (e.g. appXXXXXX)\n\nBehavior:\n  - Extracts the latest saved version into `~/.teable/refs/<appId>/`\n  - The directory is a detached snapshot: edits do NOT change the app and may be\n    overwritten by the next download. To modify the app, update it through the app builder.\n  - Env files are excluded; app secrets are never included.\n\nRequires app editing permission (base Owner/Creator) on the app's base.",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App ID to download source code for (from an @app mention or get-apps)",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app get-code [options]\n\nDownload the source code of an app into the local workspace for reading.\n\nUse this when the user mentions an app (an `@app[name](id:appXXX)` marker) and you need its\nimplementation as context: understanding how it works, referencing or imitating it, or\ncoordinating changes across apps.\n\nParameters:\n  - appId (required): The app ID (e.g. appXXXXXX)\n\nBehavior:\n  - Extracts the latest saved version into `~/.teable/refs/<appId>/`\n  - The directory is a detached snapshot: edits do NOT change the app and may be\n    overwritten by the next download. To modify the app, update it through the app builder.\n  - Env files are excluded; app secrets are never included.\n\nRequires app editing permission (base Owner/Creator) on the app's base.\n\nOptions:\n  --app-id <appId>        App ID to download source code for (from an @app mention or get-apps)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "list",
          "summary": "Get all apps in the base",
          "description": "List apps in the base with their details.\n\nParameters:\n  - search (optional): Filter apps by name (case-insensitive substring match)\n\nReturns app information:\n  - id: Unique app identifier\n  - name: Display name of the app\n  - description: Purpose and functionality description\n  - isPublished: Whether the app is published\n  - previewUrl: URL to preview the app\n\nUse this before creating or updating apps to:\n  - Check what apps already exist\n  - Find an app to update by its ID\n\nReturns an empty list if no apps exist in the base.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--search <keyword>",
              "description": "Filter apps by name (case-insensitive substring match)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app list [options]\n\nList apps in the base with their details.\n\nParameters:\n  - search (optional): Filter apps by name (case-insensitive substring match)\n\nReturns app information:\n  - id: Unique app identifier\n  - name: Display name of the app\n  - description: Purpose and functionality description\n  - isPublished: Whether the app is published\n  - previewUrl: URL to preview the app\n\nUse this before creating or updating apps to:\n  - Check what apps already exist\n  - Find an app to update by its ID\n\nReturns an empty list if no apps exist in the base.\n\nOptions:\n  --search <keyword>      Filter apps by name (case-insensitive substring match)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "login-config",
          "summary": "Configure login/authentication for an app",
          "description": "Configure login/authentication for an app.\n\nUse this tool to enable, update, or disable login for a generated app.\nWhen enabled, end-users of the app must authenticate before accessing it.\n\nParameters:\n  appId (required): The app ID\n  loginConfig (required): Login configuration object, or null to disable\n\nloginConfig fields:\n  enabled: true to enable login, false to disable\n  userTableId: table ID that stores user records\n  emailFieldId: field ID of the email column in the user table\n  providers: array of login providers, each with a \"type\" field:\n    - { \"type\": \"email-otp\" }: Email one-time password login\n    - { \"type\": \"google\" }: Google OAuth login\n    - { \"type\": \"teable\" }: Teable OAuth login\n  access (optional): access control configuration:\n    - mode: \"open\" (anyone can sign up), \"domain\" (restrict to email domains), \"existing-only\" (only existing users)\n    - domains: array of allowed email domains (only for \"domain\" mode)\n\nExamples:\n  Enable email OTP: { enabled: true, userTableId: \"tblXXX\", emailFieldId: \"fldXXX\", providers: [{ type: \"email-otp\" }] }\n  Disable login: null",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "The ID of the app to configure login for",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--login-config <json>",
              "description": "Login configuration object, or null to disable login entirely",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app login-config [options]\n\nConfigure login/authentication for an app.\n\nUse this tool to enable, update, or disable login for a generated app.\nWhen enabled, end-users of the app must authenticate before accessing it.\n\nParameters:\n  appId (required): The app ID\n  loginConfig (required): Login configuration object, or null to disable\n\nloginConfig fields:\n  enabled: true to enable login, false to disable\n  userTableId: table ID that stores user records\n  emailFieldId: field ID of the email column in the user table\n  providers: array of login providers, each with a \"type\" field:\n    - { \"type\": \"email-otp\" }: Email one-time password login\n    - { \"type\": \"google\" }: Google OAuth login\n    - { \"type\": \"teable\" }: Teable OAuth login\n  access (optional): access control configuration:\n    - mode: \"open\" (anyone can sign up), \"domain\" (restrict to email domains), \"existing-only\" (only\nexisting users)\n    - domains: array of allowed email domains (only for \"domain\" mode)\n\nExamples:\n  Enable email OTP: { enabled: true, userTableId: \"tblXXX\", emailFieldId: \"fldXXX\", providers: [{\ntype: \"email-otp\" }] }\n  Disable login: null\n\nOptions:\n  --app-id <appId>        The ID of the app to configure login for\n  --login-config <json>   Login configuration object, or null to disable login entirely\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "publish",
          "summary": "Publish an app. Returns success/failed when final; if it returns deploying, poll app status until success/failed",
          "description": "Publish an app. The result may be final (\"success\" or \"failed\") or in progress (\"deploying\"). If it returns \"deploying\", call `teable app status --app-id <appId>` until it returns \"success\" or \"failed\".",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App id (appXXXX)",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app publish [options]\n\nPublish an app. The result may be final (\"success\" or \"failed\") or in progress (\"deploying\"). If it\nreturns \"deploying\", call `teable app status --app-id <appId>` until it returns \"success\" or\n\"failed\".\n\nOptions:\n  --app-id <appId>        App id (appXXXX)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "status",
          "summary": "Check app publish status after publish returns deploying",
          "description": "Check app publish/deployment status after publish returns \"deploying\".",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App id (appXXXX)",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app status [options]\n\nCheck app publish/deployment status after publish returns \"deploying\".\n\nOptions:\n  --app-id <appId>        App id (appXXXX)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "unpublish",
          "summary": "Unpublish an app",
          "description": "Unpublish an app.",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App id (appXXXX)",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app unpublish [options]\n\nUnpublish an app.\n\nOptions:\n  --app-id <appId>        App id (appXXXX)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "update",
          "summary": "Create or update an app with AI",
          "description": "Build or modify a web application with Teable integration.\n\nActions:\n  create: Create a new app\n  update: Modify an existing app\n\nParameters:\n  action: create or update (required)\n  name: App name (optional)\n  prompt: App-building prompt for teable app create/update CLI (required)\n  appId: App ID to update (required for update action)\n  tableIds: Table IDs to include in app context (optional)\n  attachmentTokens: Image tokens for UI reference (optional)\n\nImportant rules for prompt:\n  First complete any requested setup, table inspection, analysis, or data changes.\n  Do not pass setup, analysis, or data-seeding steps to App Builder.\n  Include only the app purpose, UI/workflow, explicit constraints, relevant table IDs, and verified data context.\n  Keep the user intent without adding extra features or details.\n  Do not use markdown formatting in the prompt.\n  Do not specify technology stack unless the user explicitly requests it.\n\nWorkflow:\n  Use get-apps tool first to check existing apps.\n  If a matching app exists, use update action.\n  Otherwise, use create action.",
          "options": [
            {
              "flags": "--app-id <appId>",
              "description": "App ID to update (required for \"update\" action)",
              "required": true
            },
            {
              "flags": "--attachment-tokens <jsonArray>",
              "description": "Optional array of attachment tokens (screenshots, designs, etc.)"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "Folder to place the new app in (`folderId` from Active Tab Meta); omit for base root."
            },
            {
              "flags": "--name <name>",
              "description": "Name for the app"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--prompt <prompt>",
              "description": "App-building prompt. Include only app goals, constraints, and verified data context.",
              "required": true
            },
            {
              "flags": "--table-ids <jsonArray>",
              "description": "Optional array of table IDs to include in the app context"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable app update [options]\n\nBuild or modify a web application with Teable integration.\n\nActions:\n  create: Create a new app\n  update: Modify an existing app\n\nParameters:\n  action: create or update (required)\n  name: App name (optional)\n  prompt: App-building prompt for teable app create/update CLI (required)\n  appId: App ID to update (required for update action)\n  tableIds: Table IDs to include in app context (optional)\n  attachmentTokens: Image tokens for UI reference (optional)\n\nImportant rules for prompt:\n  First complete any requested setup, table inspection, analysis, or data changes.\n  Do not pass setup, analysis, or data-seeding steps to App Builder.\n  Include only the app purpose, UI/workflow, explicit constraints, relevant table IDs, and verified\ndata context.\n  Keep the user intent without adding extra features or details.\n  Do not use markdown formatting in the prompt.\n  Do not specify technology stack unless the user explicitly requests it.\n\nWorkflow:\n  Use get-apps tool first to check existing apps.\n  If a matching app exists, use update action.\n  Otherwise, use create action.\n\nOptions:\n  --prompt <prompt>                App-building prompt. Include only app goals, constraints, and\n                                   verified data context.\n  --name <name>                    Name for the app\n  --table-ids <jsonArray>          Optional array of table IDs to include in the app context\n  --attachment-tokens <jsonArray>  Optional array of attachment tokens (screenshots, designs, etc.)\n  --folder-id <folderId>           Folder to place the new app in (`folderId` from Active Tab Meta);\n                                   omit for base root.\n  --app-id <appId>                 App ID to update (required for \"update\" action)\n  -b, --base-id <baseId>           Target base ID (required, e.g. bseXXXX). Set default: `teable\n                                   config base-id <id>`.\n  --token <token>                  Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>            Teable API server base URL\n  --pretty                         Indent JSON for human readability (default: false)\n  -h, --help                       display help for command\n"
        }
      ],
      "help": "Usage: teable app [options] [command]\n\napp operations (list, get-code, create, update, delete, publish, status, unpublish, login-config,\nai-docs, ai-enable)\n\nOptions:\n  -h, --help              display help for command\n\nCommands:\n  list [options]          Get all apps in the base\n  get-code [options]      Download app source code as a local read-only context snapshot\n  create [options]        Create or update an app with AI\n  update [options]        Create or update an app with AI\n  delete [options]        Delete an app\n  publish [options]       Publish an app. Returns success/failed when final; if it returns\n                          deploying, poll app status until success/failed\n  status [options]        Check app publish status after publish returns deploying\n  unpublish [options]     Unpublish an app\n  login-config [options]  Configure login/authentication for an app\n  ai-docs [options]       How the app calls AI through Teable proxy (usage + available model keys)\n  ai-enable [options]     Enable AI access for an app (provisions the AI proxy key)\n  help [command]          display help for command\n\nExamples:\n  teable app list\n  teable app get-code --app-id appXXXX\n  teable app create --prompt \"A task tracker with status board\"\n  teable app update --app-id appXXXX --prompt \"Add a dark mode toggle\"\n  teable app delete --app-id appXXXX\n  teable app publish --app-id appXXXX\n  teable app status --app-id appXXXX\n  teable app unpublish --app-id appXXXX\n  teable app login-config --app-id appXXXX --login-config '{\"enabled\":true,\"userTableId\":\"tblXXX\",\"emailFieldId\":\"fldXXX\",\"providers\":[{\"type\":\"email-otp\"}]}'\n  teable app ai-docs\n  teable app ai-enable --app-id appXXXX\n"
    },
    {
      "name": "auth",
      "aliases": [
        "init"
      ],
      "description": "Manage auth for teable",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable endpoint",
          "default": "https://app.teable.ai"
        },
        {
          "flags": "--path <dir>",
          "description": "Save config into <dir>/.teable/cli/config.json instead of the user-level path (e.g. --path .)"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token"
        },
        {
          "flags": "-f, --force",
          "description": "Overwrite config file if it already exists",
          "default": false
        }
      ],
      "subcommands": [
        {
          "name": "login",
          "description": "Login via browser using OAuth (Authorization Code + PKCE)",
          "options": [
            {
              "flags": "--client-id <clientId>",
              "description": "OAuth client ID",
              "default": "clttckxmg4deadomjhs"
            },
            {
              "flags": "--device-code",
              "description": "Authorize with a one-time code entered in a browser, without a local callback port"
            },
            {
              "flags": "--scopes <scopes>",
              "description": "Comma-separated scopes to request"
            }
          ],
          "help": "Usage: teable auth login [options]\n\nLogin via browser using OAuth (Authorization Code + PKCE)\n\nOptions:\n  --client-id <clientId>  OAuth client ID (default: \"clttckxmg4deadomjhs\")\n  --scopes <scopes>       Comma-separated scopes to request\n  --device-code           Authorize with a one-time code entered in a browser, without a local\n                          callback port\n  -h, --help              display help for command\n\nUse `--endpoint <url>` to log in against a custom deployment.\nWhen this CLI runs where your browser cannot reach its 127.0.0.1 — a cloud IDE, an SSH\nsession, a container — use `--device-code`.\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "logout",
          "description": "Clear saved OAuth credentials",
          "help": "Usage: teable auth logout [options]\n\nClear saved OAuth credentials\n\nOptions:\n  -h, --help  display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "status",
          "description": "Show current auth status and endpoint",
          "help": "Usage: teable auth status [options]\n\nShow current auth status and endpoint\n\nOptions:\n  -h, --help  display help for command\n",
          "availability": "managed-sandbox-disabled"
        }
      ],
      "help": "Usage: teable auth|init [options] [command]\n\nManage auth for teable\n\nOptions:\n  -f, --force            Overwrite config file if it already exists (default: false)\n  --path <dir>           Save config into <dir>/.teable/cli/config.json instead of the user-level\n                         path (e.g. --path .)\n  --token <token>        Personal access token\n  --endpoint <endpoint>  Teable endpoint (default: \"https://app.teable.ai\")\n  -h, --help             display help for command\n\nCommands:\n  login [options]        Login via browser using OAuth (Authorization Code + PKCE)\n  logout                 Clear saved OAuth credentials\n  status                 Show current auth status and endpoint\n",
      "availability": "managed-sandbox-disabled"
    },
    {
      "name": "authority",
      "description": "authority operations (get, export, diff, apply, enable, disable, role-list, role-get, role-delete, role-duplicate)",
      "subcommands": [
        {
          "name": "apply",
          "summary": "Apply an authority config file: reconcile live state to match it",
          "description": "Apply a config file to the authority matrix: create/update roles, members,\ndefault role and admins until the live state matches the file.\nALWAYS preview first: `--dry-run` shows exactly what apply would do —\nincluding pending --prune deletions — and runs the same preflight guards;\nplain `authority diff` only compares state and does not model --prune.\n\nRoles that exist on the server but not in the file are KEPT unless --prune\nis passed. Keys absent from the file are left unchanged (managed-key rule).\nThe matrix on/off state is never touched — use `authority enable/disable`.\n\nApply is not atomic: changes run in order (role creation happens before the\ndefault-role change) and stop at the first API error. Re-running apply is\nsafe — already-applied changes simply no longer show up as differences.\n\nConfig-file shape and the full table-grant reference (deny values, field modes, the \"Me\" current-user filter token): see `teable authority export --help`.",
          "options": [
            {
              "flags": "--dry-run",
              "description": "Only print the pending changes, write nothing",
              "default": false
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--file <path>",
              "description": "Config file to apply (from `authority export`)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--prune",
              "description": "Also DELETE server roles that are absent from the file",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority apply [options]\n\nApply a config file to the authority matrix: create/update roles, members,\ndefault role and admins until the live state matches the file.\nALWAYS preview first: `--dry-run` shows exactly what apply would do —\nincluding pending --prune deletions — and runs the same preflight guards;\nplain `authority diff` only compares state and does not model --prune.\n\nRoles that exist on the server but not in the file are KEPT unless --prune\nis passed. Keys absent from the file are left unchanged (managed-key rule).\nThe matrix on/off state is never touched — use `authority enable/disable`.\n\nApply is not atomic: changes run in order (role creation happens before the\ndefault-role change) and stop at the first API error. Re-running apply is\nsafe — already-applied changes simply no longer show up as differences.\n\nConfig-file shape and the full table-grant reference (deny values, field modes, the \"Me\"\ncurrent-user filter token): see `teable authority export --help`.\n\nOptions:\n  --file <path>           Config file to apply (from `authority export`)\n  --dry-run               Only print the pending changes, write nothing (default: false)\n  --prune                 Also DELETE server roles that are absent from the file (default: false)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "diff",
          "summary": "Preview what an edited authority config file would change (dry run)",
          "description": "Compare a config file against the live authority matrix and print what\n`authority apply` would change, without writing anything (dry run).\nEquivalent grant spellings (e.g. access \"commenter\" vs \"editor\" plus deny)\nare compared by effect and never reported as differences.\n\n`unmanagedRoles` lists live roles absent from the file: kept by apply\nunless --prune is passed.\n\nConfig-file shape and the full table-grant reference (deny values, field modes, the \"Me\" current-user filter token): see `teable authority export --help`.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--file <path>",
              "description": "Config file to compare (from `authority export`)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority diff [options]\n\nCompare a config file against the live authority matrix and print what\n`authority apply` would change, without writing anything (dry run).\nEquivalent grant spellings (e.g. access \"commenter\" vs \"editor\" plus deny)\nare compared by effect and never reported as differences.\n\n`unmanagedRoles` lists live roles absent from the file: kept by apply\nunless --prune is passed.\n\nConfig-file shape and the full table-grant reference (deny values, field modes, the \"Me\"\ncurrent-user filter token): see `teable authority export --help`.\n\nOptions:\n  --file <path>           Config file to compare (from `authority export`)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "disable",
          "summary": "Disable the authority matrix for a base (config is kept)",
          "description": "Disable the authority matrix for a base; collaborators revert to their plain base roles. The role config is kept and takes effect again on re-enable.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority disable [options]\n\nDisable the authority matrix for a base; collaborators revert to their plain base roles. The role\nconfig is kept and takes effect again on re-enable.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "enable",
          "summary": "Enable the authority matrix for a base",
          "description": "Enable the authority matrix (advanced permissions) for a base.\n\nOnce enabled, every collaborator except the base Owner and the matrix admin\nusers is governed by it: users in enabled roles get their grants, everyone\nelse falls back to the default role — or LOSES ACCESS when no default role\nis set. The command still proceeds in that case but reports a warning;\nwhen in doubt, set \"defaultRole\" via the config-file flow (export → edit →\ndiff → apply) first.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority enable [options]\n\nEnable the authority matrix (advanced permissions) for a base.\n\nOnce enabled, every collaborator except the base Owner and the matrix admin\nusers is governed by it: users in enabled roles get their grants, everyone\nelse falls back to the default role — or LOSES ACCESS when no default role\nis set. The command still proceeds in that case but reports a warning;\nwhen in doubt, set \"defaultRole\" via the config-file flow (export → edit →\ndiff → apply) first.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "export",
          "summary": "Export authority matrix config as a JSON file (--role for one role) — start of every grant edit",
          "description": "Export the authority matrix (roles, grants, members, default role, admins)\nas one declarative JSON document — the starting point of EVERY permission\nconfig change: export → edit the file → `authority diff` to preview →\n`authority apply`.\n\nWith --role the file contains just that one role, omits the matrix-level\nkeys and carries `\"scope\": \"role\"`; such a partial file only manages that\nrole and can never prune, which keeps small edits small and safe.\n\nA role grants access TABLE BY TABLE: a table without a grant is completely\ninvisible to the role's members. Inside a granted table the ceiling is\nplatform Editor level — matrix roles can never create/delete tables or\nfields, or share views.\nOnce the matrix is enabled it applies to every collaborator except the base\nOwner and the matrix admin users. A user in multiple roles gets the UNION of\ntheir grants (most permissive wins).\n\nConfig file shape (managed-key rule: a key ABSENT from the file is left\nunchanged by diff/apply; an explicitly empty value ([] / null) is enforced.\nA full `export` writes every key; `export --role` writes a partial file that\nmanages only that role):\n  {\n    \"version\": 1,\n    \"baseId\": \"bseXXXX\",\n    \"scope\": \"role\",           // written by `export --role`; such a file can never\n                               // --prune. Omit for a full-matrix file (partiality\n                               // alone does NOT protect against --prune)\n    \"defaultRole\": \"<role id or name>\" | null,   // fallback for unassigned collaborators\n    \"admins\": [\"usrXXXX\"],                       // users that bypass the matrix\n    \"roles\": [{\n      \"id\": \"aurXXXX\",          // from export. An entry WITHOUT an id matches an\n                                // existing role by name and only creates a new\n                                // role when no live role has that name\n      \"name\": \"Sales\",\n      \"description\": \"...\",\n      \"enabled\": true,\n      \"members\": { \"users\": [\"usrXXXX\"], \"departments\": [] },\n      \"permissions\": [<table grants, see below>],\n      \"nodes\": [{\"nodeType\": \"workflow\"|\"app\", \"nodeId\": \"...\", \"enabled\": true}]\n    }]\n  }\nA table grant may carry \"enabled\": false — stored but not in effect (round-\ntripped faithfully by export/apply).\nmembers.departments needs an enterprise organization; resolve department ids\nvia `teable call-api` on GET /organization/department (find it with\n`teable search-api --query department`). User ids: `teable get-collaborators`.\nEvery top-level key follows the managed-key rule, including \"roles\" itself: a\nhand-written file with only \"defaultRole\"/\"admins\" and no \"roles\" key manages\njust those.\nThe matrix on/off state is NOT part of the file — use `authority enable/disable`.\n\nTable grant JSON shape (array; one entry per table the role may access):\n  [{\n    \"tableId\": \"tblXXXX\",\n    \"access\": \"editor\" | \"commenter\" | \"viewer\",  // capability preset (required)\n    \"deny\": [\"record|delete\", ...],   // optional: take away actions on top of the preset\n    \"views\": [\"viwXXXX\", ...],        // optional whitelist; omit = all views, [] = none\n    \"rowFilter\": { ... },             // optional: members only see matching rows\n                                      //   (same JSON syntax as view filters)\n    \"fields\": { \"fldXXXX\": \"hidden\" | \"read-only\" | [\"record|create\", ...] }\n                                      // optional per-field restriction\n  }]\naccess presets:\n  editor    — full table access: record CRUD + comment + copy, manage views,\n              import + export (the matrix ceiling; trim it with deny)\n  commenter — read + comment + copy + export; cannot change records or views\n  viewer    — read + copy + export only\ndeny values: view|create, view|delete, view|update, table|import, table|export, field|read, record|create, record|delete, record|read, record|update, record|comment, record|copy, record|archive.\nfields: \"hidden\" = field invisible to members; \"read-only\" = visible, not\neditable; an action array disables exactly those record actions\n(record|read / record|update / record|create).\nFields added to the table AFTER a field restriction set was stored stay\ninaccessible to the role; grant one explicitly with \"fldXXXX\": [].\nrowFilter syntax: see `teable get-doc --topic view.filter`.\nFor user fields the dynamic current-user filter value is the literal \"Me\"\n(capital M) — lowercase \"me\" is a plain string and matches nothing.\nA table the role should NOT access is simply omitted from the array — there\nis no \"none\" value.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--file <path>",
              "description": "Write to this file instead of stdout"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--role <idOrName>",
              "description": "Export only this role (partial file for a scoped edit)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority export [options]\n\nExport the authority matrix (roles, grants, members, default role, admins)\nas one declarative JSON document — the starting point of EVERY permission\nconfig change: export → edit the file → `authority diff` to preview →\n`authority apply`.\n\nWith --role the file contains just that one role, omits the matrix-level\nkeys and carries `\"scope\": \"role\"`; such a partial file only manages that\nrole and can never prune, which keeps small edits small and safe.\n\nA role grants access TABLE BY TABLE: a table without a grant is completely\ninvisible to the role's members. Inside a granted table the ceiling is\nplatform Editor level — matrix roles can never create/delete tables or\nfields, or share views.\nOnce the matrix is enabled it applies to every collaborator except the base\nOwner and the matrix admin users. A user in multiple roles gets the UNION of\ntheir grants (most permissive wins).\n\nConfig file shape (managed-key rule: a key ABSENT from the file is left\nunchanged by diff/apply; an explicitly empty value ([] / null) is enforced.\nA full `export` writes every key; `export --role` writes a partial file that\nmanages only that role):\n  {\n    \"version\": 1,\n    \"baseId\": \"bseXXXX\",\n    \"scope\": \"role\",           // written by `export --role`; such a file can never\n                               // --prune. Omit for a full-matrix file (partiality\n                               // alone does NOT protect against --prune)\n    \"defaultRole\": \"<role id or name>\" | null,   // fallback for unassigned collaborators\n    \"admins\": [\"usrXXXX\"],                       // users that bypass the matrix\n    \"roles\": [{\n      \"id\": \"aurXXXX\",          // from export. An entry WITHOUT an id matches an\n                                // existing role by name and only creates a new\n                                // role when no live role has that name\n      \"name\": \"Sales\",\n      \"description\": \"...\",\n      \"enabled\": true,\n      \"members\": { \"users\": [\"usrXXXX\"], \"departments\": [] },\n      \"permissions\": [<table grants, see below>],\n      \"nodes\": [{\"nodeType\": \"workflow\"|\"app\", \"nodeId\": \"...\", \"enabled\": true}]\n    }]\n  }\nA table grant may carry \"enabled\": false — stored but not in effect (round-\ntripped faithfully by export/apply).\nmembers.departments needs an enterprise organization; resolve department ids\nvia `teable call-api` on GET /organization/department (find it with\n`teable search-api --query department`). User ids: `teable get-collaborators`.\nEvery top-level key follows the managed-key rule, including \"roles\" itself: a\nhand-written file with only \"defaultRole\"/\"admins\" and no \"roles\" key manages\njust those.\nThe matrix on/off state is NOT part of the file — use `authority enable/disable`.\n\nTable grant JSON shape (array; one entry per table the role may access):\n  [{\n    \"tableId\": \"tblXXXX\",\n    \"access\": \"editor\" | \"commenter\" | \"viewer\",  // capability preset (required)\n    \"deny\": [\"record|delete\", ...],   // optional: take away actions on top of the preset\n    \"views\": [\"viwXXXX\", ...],        // optional whitelist; omit = all views, [] = none\n    \"rowFilter\": { ... },             // optional: members only see matching rows\n                                      //   (same JSON syntax as view filters)\n    \"fields\": { \"fldXXXX\": \"hidden\" | \"read-only\" | [\"record|create\", ...] }\n                                      // optional per-field restriction\n  }]\naccess presets:\n  editor    — full table access: record CRUD + comment + copy, manage views,\n              import + export (the matrix ceiling; trim it with deny)\n  commenter — read + comment + copy + export; cannot change records or views\n  viewer    — read + copy + export only\ndeny values: view|create, view|delete, view|update, table|import, table|export, field|read,\nrecord|create, record|delete, record|read, record|update, record|comment, record|copy,\nrecord|archive.\nfields: \"hidden\" = field invisible to members; \"read-only\" = visible, not\neditable; an action array disables exactly those record actions\n(record|read / record|update / record|create).\nFields added to the table AFTER a field restriction set was stored stay\ninaccessible to the role; grant one explicitly with \"fldXXXX\": [].\nrowFilter syntax: see `teable get-doc --topic view.filter`.\nFor user fields the dynamic current-user filter value is the literal \"Me\"\n(capital M) — lowercase \"me\" is a plain string and matches nothing.\nA table the role should NOT access is simply omitted from the array — there\nis no \"none\" value.\n\nOptions:\n  --file <path>           Write to this file instead of stdout\n  --role <idOrName>       Export only this role (partial file for a scoped edit)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "get",
          "summary": "Get the authority matrix (advanced permissions) config of a base",
          "description": "Get the authority matrix (advanced permissions) config of a base.\n\nReturns the matrix id, enabledTime (set = enabled), defaultRole (the authority\nrole id applied to collaborators not assigned to any role) and adminUsers\n(users that bypass all matrix restrictions). Empty response means the matrix\nhas never been configured for this base.\nRequires the EE advanced-permissions feature and base Owner/Creator access.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority get [options]\n\nGet the authority matrix (advanced permissions) config of a base.\n\nReturns the matrix id, enabledTime (set = enabled), defaultRole (the authority\nrole id applied to collaborators not assigned to any role) and adminUsers\n(users that bypass all matrix restrictions). Empty response means the matrix\nhas never been configured for this base.\nRequires the EE advanced-permissions feature and base Owner/Creator access.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "role-delete",
          "summary": "Delete an authority role and its permission config",
          "description": "Delete an authority role and its permission config. Members fall back to the default role. Deleting the default role itself is refused — repoint \"defaultRole\" via the config-file flow first.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--role-id <roleId>",
              "description": "Authority role ID (aurXXXX)",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority role-delete [options]\n\nDelete an authority role and its permission config. Members fall back to the default role. Deleting\nthe default role itself is refused — repoint \"defaultRole\" via the config-file flow first.\n\nOptions:\n  --role-id <roleId>      Authority role ID (aurXXXX)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "role-duplicate",
          "summary": "Duplicate an authority role (optionally including members)",
          "description": "Duplicate an authority role with its table/node permission config; membership is copied only with the --include-* flags. Prints the new role (including its id), optionally renamed via --name.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--include-departments",
              "description": "Copy the department assignments too",
              "default": false
            },
            {
              "flags": "--include-users",
              "description": "Copy the user assignments too",
              "default": false
            },
            {
              "flags": "--name <name>",
              "description": "Rename the duplicate right away"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--role-id <roleId>",
              "description": "Authority role ID to duplicate (aurXXXX)",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority role-duplicate [options]\n\nDuplicate an authority role with its table/node permission config; membership is copied only with\nthe --include-* flags. Prints the new role (including its id), optionally renamed via --name.\n\nOptions:\n  --role-id <roleId>      Authority role ID to duplicate (aurXXXX)\n  --name <name>           Rename the duplicate right away\n  --include-users         Copy the user assignments too (default: false)\n  --include-departments   Copy the department assignments too (default: false)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "role-get",
          "summary": "Get one authority role: members and per-table grants (intent view; --raw for API form)",
          "description": "Get one authority role: members, per-table grants and automation/app node\naccess, in the same intent shape used by the config-file flow:\n{ id, name, enabled, users, departments, permissions: [table grants], nodes }.\nA table absent from `permissions` is invisible to the role's members.\nAn entry marked \"enabled\": false is stored but NOT in effect.\nTo change grants: `authority export --role <id>` → edit → diff → apply.\nUse --raw for the untranslated API response (disabledActions form).",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--raw",
              "description": "Print the raw API response instead of the intent view",
              "default": false
            },
            {
              "flags": "--role-id <roleId>",
              "description": "Authority role ID (aurXXXX)",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority role-get [options]\n\nGet one authority role: members, per-table grants and automation/app node\naccess, in the same intent shape used by the config-file flow:\n{ id, name, enabled, users, departments, permissions: [table grants], nodes }.\nA table absent from `permissions` is invisible to the role's members.\nAn entry marked \"enabled\": false is stored but NOT in effect.\nTo change grants: `authority export --role <id>` → edit → diff → apply.\nUse --raw for the untranslated API response (disabledActions form).\n\nOptions:\n  --role-id <roleId>      Authority role ID (aurXXXX)\n  --raw                   Print the raw API response instead of the intent view (default: false)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "role-list",
          "summary": "List authority roles with their assigned users/departments",
          "description": "List authority roles of a base with their assigned users/departments. Use `authority role-get` for a role's full table/node permission config.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable authority role-list [options]\n\nList authority roles of a base with their assigned users/departments. Use `authority role-get` for a\nrole's full table/node permission config.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        }
      ],
      "help": "Usage: teable authority [options] [command]\n\nauthority operations (get, export, diff, apply, enable, disable, role-list, role-get, role-delete,\nrole-duplicate)\n\nOptions:\n  -h, --help                display help for command\n\nCommands:\n  get [options]             Get the authority matrix (advanced permissions) config of a base\n  export [options]          Export authority matrix config as a JSON file (--role for one role) —\n                            start of every grant edit\n  diff [options]            Preview what an edited authority config file would change (dry run)\n  apply [options]           Apply an authority config file: reconcile live state to match it\n  enable [options]          Enable the authority matrix for a base\n  disable [options]         Disable the authority matrix for a base (config is kept)\n  role-list [options]       List authority roles with their assigned users/departments\n  role-get [options]        Get one authority role: members and per-table grants (intent view; --raw\n                            for API form)\n  role-delete [options]     Delete an authority role and its permission config\n  role-duplicate [options]  Duplicate an authority role (optionally including members)\n  help [command]            display help for command\n\nExamples:\n  teable authority get -b bseXXXX\n  # ALL config changes (roles, grants, members, default role, admins) go\n  # export → edit → diff → apply:\n  teable authority export -b bseXXXX --file authority.json\n  teable authority export -b bseXXXX --role \"Sales\" --file sales.json   # scoped small edit\n  teable authority diff -b bseXXXX --file authority.json\n  teable authority apply -b bseXXXX --file authority.json\n  teable authority enable -b bseXXXX\n"
    },
    {
      "name": "automation",
      "description": "automation operations (list, get, update, activate, setup-trigger, delete, test-node, delete-node, generate-script, generate-flowchart, get-script-input, get-runs, get-run)",
      "subcommands": [
        {
          "name": "activate",
          "summary": "Activate(enable draft) or deactivate automation",
          "description": "Activate, deactivate, or discard changes to an automation workflow.\n\n🔄 Workflow Lifecycle:\n   Edit → Test → Activate → Running -> Edit -> Test -> Activate -> ...\n                    ↓\n              Deactivate → Paused\n\n⚠️ IMPORTANT - Draft System:\n- All edits create a DRAFT version (not immediately effective)\n- You MUST call activate to apply draft changes to the running automation\n\n📋 Operations:\n\n1️⃣  activate - Enable automation AND apply all draft changes\n\n2️⃣  deactivate - Disable the automation\n\n3️⃣  discard - Discard draft changes and revert to last active version\n\n⚠️  Risk Considerations:\n- Activating in production will start sending real emails/API calls\n- Consider using askForConfirmation before activating in production\n- Deactivating stops all automated actions immediately\n\n💡 Best Practices:\n- Monitor the first few runs after activation",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--method <method>",
              "description": "Operation to perform on the workflow",
              "required": true,
              "choices": [
                "activate",
                "deactivate",
                "discard"
              ]
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation activate [options]\n\nActivate, deactivate, or discard changes to an automation workflow.\n\n🔄 Workflow Lifecycle:\n   Edit → Test → Activate → Running -> Edit -> Test -> Activate -> ...\n                    ↓\n              Deactivate → Paused\n\n⚠️ IMPORTANT - Draft System:\n- All edits create a DRAFT version (not immediately effective)\n- You MUST call activate to apply draft changes to the running automation\n\n📋 Operations:\n\n1️⃣  activate - Enable automation AND apply all draft changes\n\n2️⃣  deactivate - Disable the automation\n\n3️⃣  discard - Discard draft changes and revert to last active version\n\n⚠️  Risk Considerations:\n- Activating in production will start sending real emails/API calls\n- Consider using askForConfirmation before activating in production\n- Deactivating stops all automated actions immediately\n\n💡 Best Practices:\n- Monitor the first few runs after activation\n\nOptions:\n  --workflow-id <workflowId>  The ID of the automation workflow\n  --method <method>           Operation to perform on the workflow (choices: \"activate\",\n                              \"deactivate\", \"discard\")\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete an automation workflow",
          "description": "Delete an automation workflow (moves it to trash), including its trigger, action and logic nodes.\n\nParameters:\n  - workflowId (required): The ID of the automation workflow to delete\n\n⚠️ Warning: This operation is destructive. Verify workflowId before deletion. The workflow is deactivated and can be restored from the base trash.\nTo remove a single node instead of the whole workflow, use delete-automation-node.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow to delete",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation delete [options]\n\nDelete an automation workflow (moves it to trash), including its trigger, action and logic nodes.\n\nParameters:\n  - workflowId (required): The ID of the automation workflow to delete\n\n⚠️ Warning: This operation is destructive. Verify workflowId before deletion. The workflow is\ndeactivated and can be restored from the base trash.\nTo remove a single node instead of the whole workflow, use delete-automation-node.\n\nOptions:\n  --workflow-id <workflowId>  The ID of the automation workflow to delete\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "delete-node",
          "summary": "Delete an action or logic node from automation workflow",
          "description": "Delete an action or logic node from an automation workflow.\n\nCannot delete trigger nodes. Need get automation first to see the workflow structure and node IDs.\n\n⚠️ Deletion is immediate on the draft. If the workflow is active, need activate automation to publish changes.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--node-id <nodeId>",
              "description": "The node ID to delete (action or logic node)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The workflow ID",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation delete-node [options]\n\nDelete an action or logic node from an automation workflow.\n\nCannot delete trigger nodes. Need get automation first to see the workflow structure and node IDs.\n\n⚠️ Deletion is immediate on the draft. If the workflow is active, need activate automation to\npublish changes.\n\nOptions:\n  --workflow-id <workflowId>  The workflow ID\n  --node-id <nodeId>          The node ID to delete (action or logic node)\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "generate-flowchart",
          "summary": "Generate flowchart for automation script action node",
          "description": "Generate a structured flowchart from script analysis and persist it to the action config.\n\nGuidelines:\n- Each node = one logical operation, NOT one line of code. Group sequential API calls, assignments, or transforms into a single step.\n- Prefer 5-12 nodes for simple scripts; complex scripts with many branches/loops may need more.\n- Keep labels short (3-8 words). Omit node description unless the label alone is ambiguous.\n- Label edges only for branches (\"Yes\", \"No\", \"Error\", \"Each item\"); omit label for default/sequential flow.\n- Always include exactly one \"start\" node and one \"end\" node.\n\nExample (linear script):\n  GOOD: start → \"Fetch records\" → \"Transform data\" → \"Write results\" → end\n  BAD:  start → \"Declare vars\" → \"Set headers\" → \"Call fetch\" → \"Parse JSON\" → \"Loop row\" → …\n\nExample (branching script):\n  start → \"Validate input\" → condition(\"Has records?\") →[Yes]→ \"Process records\" → \"Save results\" → end\n                                                        →[No]→ \"Log empty warning\" → end",
          "options": [
            {
              "flags": "--action-id <actionId>",
              "description": "The action ID to update",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--flowchart <json>",
              "description": "JSON: nodes (id, type, label) + edges (source, target). Types: start|end|step|condition|loop|tryCatch",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The workflow ID",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation generate-flowchart [options]\n\nGenerate a structured flowchart from script analysis and persist it to the action config.\n\nGuidelines:\n- Each node = one logical operation, NOT one line of code. Group sequential API calls, assignments,\nor transforms into a single step.\n- Prefer 5-12 nodes for simple scripts; complex scripts with many branches/loops may need more.\n- Keep labels short (3-8 words). Omit node description unless the label alone is ambiguous.\n- Label edges only for branches (\"Yes\", \"No\", \"Error\", \"Each item\"); omit label for\ndefault/sequential flow.\n- Always include exactly one \"start\" node and one \"end\" node.\n\nExample (linear script):\n  GOOD: start → \"Fetch records\" → \"Transform data\" → \"Write results\" → end\n  BAD:  start → \"Declare vars\" → \"Set headers\" → \"Call fetch\" → \"Parse JSON\" → \"Loop row\" → …\n\nExample (branching script):\n  start → \"Validate input\" → condition(\"Has records?\") →[Yes]→ \"Process records\" → \"Save results\" →\nend\n                                                        →[No]→ \"Log empty warning\" → end\n\nOptions:\n  --workflow-id <workflowId>  The workflow ID\n  --action-id <actionId>      The action ID to update\n  --flowchart <json>          JSON: nodes (id, type, label) + edges (source, target). Types:\n                              start|end|step|condition|loop|tryCatch\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "generate-script",
          "summary": "Generate script action code for automation",
          "description": "Update script action with JavaScript code.\n\n⚠️ Read docs first: get-doc topic=\"automation.api\"\n⚠️ For sending emails: get-doc topic=\"automation.send-email\"\n⚠️ For AI: get-doc topic=\"automation.ai\"\n⚠️ For Slack: get-doc topic=\"integration.slack\"\n\n🔧 Available in script:\n- process.env.AUTOMATION_TOKEN - Auth token for API calls\n  ⚠️ NEVER use TEABLE_TOKEN — it does NOT exist in automation scripts\n- process.env.PUBLIC_ORIGIN - Base URL for API\n- input[triggerId] - Trigger data (record, fields, etc.)\n- input.integrations[\"<id>\"] - Integration info (id, name, provider)\n- output.set(key, value) - Return data for next action\n\n🔗 External Integrations:\n1. Call get-user-integrations to get integration IDs\n2. Pass integrations array: [{ id: \"<integration-id>\" }]\n3. Read integration doc for token access and API usage patterns\n\n📡 API Calls (use fetch with token):\n\n// Send Email\nawait fetch(process.env.PUBLIC_ORIGIN + \"/api/automation/runtime/email\", {\n  method: \"POST\",\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}`, \"Content-Type\": \"application/json\" },\n  body: JSON.stringify({ to: \"user@example.com\", subject: \"Hi\", body: \"Hello\" })\n});\n\n// Get Records\nawait fetch(process.env.PUBLIC_ORIGIN + `/api/table/${tableId}/record`, {\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}` }\n});\n\n// Create Records\nawait fetch(process.env.PUBLIC_ORIGIN + `/api/table/${tableId}/record`, {\n  method: \"POST\",\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}`, \"Content-Type\": \"application/json\" },\n  body: JSON.stringify({ records: [{ fields: { Name: \"Test\" } }] })\n});\n\n⚠️ Rules: Use built-in fetch(), NEVER node-fetch\n💡 After writing code, test with test-automation-node",
          "options": [
            {
              "flags": "--action-id <actionId>",
              "description": "The action ID to update",
              "required": true
            },
            {
              "flags": "--code <code>",
              "description": "The JavaScript code to execute",
              "required": true
            },
            {
              "flags": "--dependencies <jsonArray>",
              "description": "npm dependencies required by the code"
            },
            {
              "flags": "--description <description>",
              "description": "A brief description of what the code does"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--integrations <jsonArray>"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The workflow ID",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation generate-script [options]\n\nUpdate script action with JavaScript code.\n\n⚠️ Read docs first: get-doc topic=\"automation.api\"\n⚠️ For sending emails: get-doc topic=\"automation.send-email\"\n⚠️ For AI: get-doc topic=\"automation.ai\"\n⚠️ For Slack: get-doc topic=\"integration.slack\"\n\n🔧 Available in script:\n- process.env.AUTOMATION_TOKEN - Auth token for API calls\n  ⚠️ NEVER use TEABLE_TOKEN — it does NOT exist in automation scripts\n- process.env.PUBLIC_ORIGIN - Base URL for API\n- input[triggerId] - Trigger data (record, fields, etc.)\n- input.integrations[\"<id>\"] - Integration info (id, name, provider)\n- output.set(key, value) - Return data for next action\n\n🔗 External Integrations:\n1. Call get-user-integrations to get integration IDs\n2. Pass integrations array: [{ id: \"<integration-id>\" }]\n3. Read integration doc for token access and API usage patterns\n\n📡 API Calls (use fetch with token):\n\n// Send Email\nawait fetch(process.env.PUBLIC_ORIGIN + \"/api/automation/runtime/email\", {\n  method: \"POST\",\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}`, \"Content-Type\":\n\"application/json\" },\n  body: JSON.stringify({ to: \"user@example.com\", subject: \"Hi\", body: \"Hello\" })\n});\n\n// Get Records\nawait fetch(process.env.PUBLIC_ORIGIN + `/api/table/${tableId}/record`, {\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}` }\n});\n\n// Create Records\nawait fetch(process.env.PUBLIC_ORIGIN + `/api/table/${tableId}/record`, {\n  method: \"POST\",\n  headers: { Authorization: `Bearer ${process.env.AUTOMATION_TOKEN}`, \"Content-Type\":\n\"application/json\" },\n  body: JSON.stringify({ records: [{ fields: { Name: \"Test\" } }] })\n});\n\n⚠️ Rules: Use built-in fetch(), NEVER node-fetch\n💡 After writing code, test with test-automation-node\n\nOptions:\n  --workflow-id <workflowId>   The workflow ID\n  --action-id <actionId>       The action ID to update\n  --code <code>                The JavaScript code to execute\n  --description <description>  A brief description of what the code does\n  --dependencies <jsonArray>   npm dependencies required by the code\n  --integrations <jsonArray>\n  -b, --base-id <baseId>       Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                               base-id <id>`.\n  --token <token>              Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>        Teable API server base URL\n  --pretty                     Indent JSON for human readability (default: false)\n  -h, --help                   display help for command\n"
        },
        {
          "name": "get",
          "summary": "Get detailed automation workflow",
          "description": "🔍 Get detailed information about a specific automation workflow.\n\nReturns complete workflow information:\n- Workflow metadata (id, name, description, status)\n- Draft status (hasDraft) - whether unpublished changes exist\n- Edges: execution flow graph (source→target) showing node execution order and dependencies\n- Trigger configuration and output variables\n- All action nodes with full config (script code, email body/subject, HTTP url/method/body, record fields, etc.)\n- Logic/control nodes: conditions (if/else branches), repeat loops, and structural markers\n- Recent execution history\n\nParameters:\n- workflowId: The workflow ID to query (required)\n- includeActiveSnapshot: Set true to also get the published action configs for comparison (default: false)\n\n⚠️ Draft System:\n- When hasDraft=true on an active workflow, the running version differs from the draft\n- Set includeActiveSnapshot=true to see both versions and compare\n- Use activate-automation method=\"activate\" to publish draft changes\n- Use activate-automation method=\"discard\" to discard draft changes\n\n💡 Use this before modifying an automation to:\n- See current trigger configuration\n- Understand all action nodes and their full config\n- Check available input variables\n- Review recent run status",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--include-active-snapshot",
              "description": "If true and the workflow is active with draft changes, also fetches the published (active) action configs for comparison with the draft."
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow to query",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation get [options]\n\n🔍 Get detailed information about a specific automation workflow.\n\nReturns complete workflow information:\n- Workflow metadata (id, name, description, status)\n- Draft status (hasDraft) - whether unpublished changes exist\n- Edges: execution flow graph (source→target) showing node execution order and dependencies\n- Trigger configuration and output variables\n- All action nodes with full config (script code, email body/subject, HTTP url/method/body, record\nfields, etc.)\n- Logic/control nodes: conditions (if/else branches), repeat loops, and structural markers\n- Recent execution history\n\nParameters:\n- workflowId: The workflow ID to query (required)\n- includeActiveSnapshot: Set true to also get the published action configs for comparison (default:\nfalse)\n\n⚠️ Draft System:\n- When hasDraft=true on an active workflow, the running version differs from the draft\n- Set includeActiveSnapshot=true to see both versions and compare\n- Use activate-automation method=\"activate\" to publish draft changes\n- Use activate-automation method=\"discard\" to discard draft changes\n\n💡 Use this before modifying an automation to:\n- See current trigger configuration\n- Understand all action nodes and their full config\n- Check available input variables\n- Review recent run status\n\nOptions:\n  --workflow-id <workflowId>  The ID of the automation workflow to query\n  --include-active-snapshot   If true and the workflow is active with draft changes, also fetches\n                              the published (active) action configs for comparison with the draft.\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "get-run",
          "summary": "Get automation run detail",
          "description": "🔍 Get step-level detail of a single automation run.\n\nParameters:\n- workflowId: The workflow ID\n- runId: The run ID (obtain from get-automation-runs)\n\n💡 Use cases:\n- Inspect which steps succeeded or failed in a run\n- Debug a failed run by examining step inputs, outputs, and error messages\n- Understand execution timing via spent field",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--run-id <runId>",
              "description": "The ID of the run to inspect",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation get-run [options]\n\n🔍 Get step-level detail of a single automation run.\n\nParameters:\n- workflowId: The workflow ID\n- runId: The run ID (obtain from get-automation-runs)\n\n💡 Use cases:\n- Inspect which steps succeeded or failed in a run\n- Debug a failed run by examining step inputs, outputs, and error messages\n- Understand execution timing via spent field\n\nOptions:\n  --workflow-id <workflowId>  The ID of the automation workflow\n  --run-id <runId>            The ID of the run to inspect\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "get-runs",
          "summary": "Get automation run history",
          "description": "📜 Get run history of an automation workflow.\n\nParameters:\n- workflowId: The workflow ID\n- skip: Number of runs to skip (pagination)\n- take: Number of runs to return (max: 10)\n- status: Filter by run status (success | failed | running | canceled | pending)\n- cursor: Continuation cursor; when a response returns nextCursor, pass it back here instead of increasing skip\n\n💡 Use cases:\n- Verify automation executed correctly after triggering\n- Debug failed runs by checking error messages (filter by status=\"failed\" to quickly find failures)\n- Monitor automation activity",
          "options": [
            {
              "flags": "--cursor <cursor>",
              "description": "Continuation cursor from a previous response (nextCursor). When present, pass it instead of increasing skip — offset paging past an archived-history page can skip runs."
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--skip <number>",
              "description": "Number of runs to skip (pagination)"
            },
            {
              "flags": "--status <status>",
              "description": "Filter runs by status (e.g. \"failed\" to only get failed runs)",
              "choices": [
                "success",
                "failed",
                "running",
                "canceled",
                "pending"
              ]
            },
            {
              "flags": "--take <number>",
              "description": "Number of runs to return (max: 10)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation get-runs [options]\n\n📜 Get run history of an automation workflow.\n\nParameters:\n- workflowId: The workflow ID\n- skip: Number of runs to skip (pagination)\n- take: Number of runs to return (max: 10)\n- status: Filter by run status (success | failed | running | canceled | pending)\n- cursor: Continuation cursor; when a response returns nextCursor, pass it back here instead of\nincreasing skip\n\n💡 Use cases:\n- Verify automation executed correctly after triggering\n- Debug failed runs by checking error messages (filter by status=\"failed\" to quickly find failures)\n- Monitor automation activity\n\nOptions:\n  --workflow-id <workflowId>  The ID of the automation workflow\n  --skip <number>             Number of runs to skip (pagination)\n  --take <number>             Number of runs to return (max: 10)\n  --cursor <cursor>           Continuation cursor from a previous response (nextCursor). When\n                              present, pass it instead of increasing skip — offset paging past an\n                              archived-history page can skip runs.\n  --status <status>           Filter runs by status (e.g. \"failed\" to only get failed runs)\n                              (choices: \"success\", \"failed\", \"running\", \"canceled\", \"pending\")\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "get-script-input",
          "summary": "Get script input from workflow actions",
          "description": "Get the script input data from previous workflow actions. \nThis tool provides the input object for script actions, where each key is a nodeId and the value represents the output from that workflow action. \nThe input object structure: \n- input.nodeId - Access a specific node's simplified output object \n- input.nodeId.records - Access records array from a node\n- input.integrations - Access integrations object from the script action, you can access the integration by the key of the integration, in context of the script action\nYou can access data using dot notation like input.nodeId.fieldName or input.nodeId.records. \nThe complex workflow variable structure has been flattened into simple key-value pairs for easier access.",
          "options": [
            {
              "flags": "--action-id <actionId>",
              "description": "The ID of the action to get the script input from",
              "required": true
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the workflow(automation) to get the script input from",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation get-script-input [options]\n\nGet the script input data from previous workflow actions.\nThis tool provides the input object for script actions, where each key is a nodeId and the value\nrepresents the output from that workflow action.\nThe input object structure:\n- input.nodeId - Access a specific node's simplified output object\n- input.nodeId.records - Access records array from a node\n- input.integrations - Access integrations object from the script action, you can access the\nintegration by the key of the integration, in context of the script action\nYou can access data using dot notation like input.nodeId.fieldName or input.nodeId.records.\nThe complex workflow variable structure has been flattened into simple key-value pairs for easier\naccess.\n\nOptions:\n  --workflow-id <workflowId>  The ID of the workflow(automation) to get the script input from\n  --action-id <actionId>      The ID of the action to get the script input from\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "list",
          "summary": "List all automations in the base",
          "description": "📋 Get all automation workflows in the base.\n\nReturns a list of automations with:\n- id, name, description\n- isActive (deployment status)\n- trigger type and summary\n- action count\n- last execution status\n\nUse this to:\n• Check existing automations before creating new ones\n• Find automation to update\n• Understand current automation landscape\n\nExample use cases:\n- \"Show me all automation workflows\"\n- \"List active automations\"\n- \"Check if email notification automation exists\"",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation list [options]\n\n📋 Get all automation workflows in the base.\n\nReturns a list of automations with:\n- id, name, description\n- isActive (deployment status)\n- trigger type and summary\n- action count\n- last execution status\n\nUse this to:\n• Check existing automations before creating new ones\n• Find automation to update\n• Understand current automation landscape\n\nExample use cases:\n- \"Show me all automation workflows\"\n- \"List active automations\"\n- \"Check if email notification automation exists\"\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "setup-trigger",
          "summary": "Setup automation trigger configuration triggers: recordCreated, recordUpdated, recordMatchesConditions, buttonClick, formSubmitted, scheduledTime, webhook, emailReceived",
          "description": "Create or update an automation workflow with its trigger.\n\n⚠️ REQUIRED: Before using this tool, you MUST read documentation first:\n   get-doc topic=\"automation.trigger\" - Trigger configuration details\n   get-doc topic=\"automation.api\" - REST API reference for scripts\n\n🎯 Build an automation:\n1. Read docs with get-doc (REQUIRED)\n2. Call this tool (new: creates trigger + script action; existing: updates only the trigger)\n3. Use generate-script-action with the returned actionId to add code (updates return no actionId — target an existing action or set createScriptAction: true)\n4. Test with test-automation-node\n5. Activate with activate-automation\n\n📋 Trigger Types:\n• recordCreated - When record created (requires tableId)\n• recordUpdated - When record updated (requires tableId)\n• recordMatchesConditions - When record matches filter conditions on create/update (requires tableId + filter)\n• formSubmitted - When form submitted (requires tableId + formId)\n• scheduledTime - At scheduled time (requires scheduleConfig)\n• buttonClick - When button clicked (requires tableId + fieldId)\n• webhook - When webhook request is received (optional bearer token auth)\n• emailReceived - When email received (requires emailReceivedConfig.connectionType: imap)\n\n📤 Returns:\n• workflowId, triggerId, actionId\n• inputSchema - Available variables for your script\n• scriptUsage - Example code for accessing trigger data",
          "options": [
            {
              "flags": "--create-script-action",
              "description": "Add an empty script action after the trigger. Default: true for new workflows, false when updating (pass true to add a node to an existing workflow)."
            },
            {
              "flags": "--description <description>",
              "description": "Description of what this automation does"
            },
            {
              "flags": "--email-received-config <jsonObject>",
              "description": "Email configuration (for emailReceived trigger)"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--field-id <fieldId>",
              "description": "Button field ID (for buttonClick trigger)"
            },
            {
              "flags": "--filter <jsonObject>",
              "description": "Filter conditions. REQUIRED for recordMatchesConditions, optional for recordCreated/recordUpdated. For detailed filter syntax, see `view.filter` topic"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "Folder to place the new workflow in (`folderId` from Active Tab Meta); omit for base root."
            },
            {
              "flags": "--form-id <formId>",
              "description": "Form ID (required for formSubmitted trigger)"
            },
            {
              "flags": "--name <name>",
              "description": "Name for the workflow"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--schedule-config <jsonObject>",
              "description": "Schedule configuration (for scheduledTime)"
            },
            {
              "flags": "--table-id <tableId>",
              "description": "Table ID (required for record/form/button triggers)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--trigger-type <triggerType>",
              "description": "Type of trigger",
              "required": true,
              "choices": [
                "recordCreated",
                "recordUpdated",
                "recordMatchesConditions",
                "formSubmitted",
                "scheduledTime",
                "buttonClick",
                "webhook",
                "emailReceived"
              ]
            },
            {
              "flags": "--view-id <viewId>",
              "description": "View ID (optional for record triggers)"
            },
            {
              "flags": "--watch-field-ids <jsonArray>",
              "description": "Field IDs to watch for changes (for recordUpdated)"
            },
            {
              "flags": "--webhook-authorization <jsonObject>",
              "description": "Webhook security settings (for webhook trigger)"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "Existing workflow ID to update. If not provided, creates a new workflow"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation setup-trigger [options]\n\nCreate or update an automation workflow with its trigger.\n\n⚠️ REQUIRED: Before using this tool, you MUST read documentation first:\n   get-doc topic=\"automation.trigger\" - Trigger configuration details\n   get-doc topic=\"automation.api\" - REST API reference for scripts\n\n🎯 Build an automation:\n1. Read docs with get-doc (REQUIRED)\n2. Call this tool (new: creates trigger + script action; existing: updates only the trigger)\n3. Use generate-script-action with the returned actionId to add code (updates return no actionId —\ntarget an existing action or set createScriptAction: true)\n4. Test with test-automation-node\n5. Activate with activate-automation\n\n📋 Trigger Types:\n• recordCreated - When record created (requires tableId)\n• recordUpdated - When record updated (requires tableId)\n• recordMatchesConditions - When record matches filter conditions on create/update (requires tableId\n+ filter)\n• formSubmitted - When form submitted (requires tableId + formId)\n• scheduledTime - At scheduled time (requires scheduleConfig)\n• buttonClick - When button clicked (requires tableId + fieldId)\n• webhook - When webhook request is received (optional bearer token auth)\n• emailReceived - When email received (requires emailReceivedConfig.connectionType: imap)\n\n📤 Returns:\n• workflowId, triggerId, actionId\n• inputSchema - Available variables for your script\n• scriptUsage - Example code for accessing trigger data\n\nOptions:\n  --trigger-type <triggerType>          Type of trigger (choices: \"recordCreated\", \"recordUpdated\",\n                                        \"recordMatchesConditions\", \"formSubmitted\", \"scheduledTime\",\n                                        \"buttonClick\", \"webhook\", \"emailReceived\")\n  --workflow-id <workflowId>            Existing workflow ID to update. If not provided, creates a\n                                        new workflow\n  --name <name>                         Name for the workflow\n  --description <description>           Description of what this automation does\n  --table-id <tableId>                  Table ID (required for record/form/button triggers)\n  --view-id <viewId>                    View ID (optional for record triggers)\n  --form-id <formId>                    Form ID (required for formSubmitted trigger)\n  --watch-field-ids <jsonArray>         Field IDs to watch for changes (for recordUpdated)\n  --field-id <fieldId>                  Button field ID (for buttonClick trigger)\n  --filter <jsonObject>                 Filter conditions. REQUIRED for recordMatchesConditions,\n                                        optional for recordCreated/recordUpdated. For detailed\n                                        filter syntax, see `view.filter` topic\n  --schedule-config <jsonObject>        Schedule configuration (for scheduledTime)\n  --webhook-authorization <jsonObject>  Webhook security settings (for webhook trigger)\n  --email-received-config <jsonObject>  Email configuration (for emailReceived trigger)\n  --create-script-action                Add an empty script action after the trigger. Default: true\n                                        for new workflows, false when updating (pass true to add a\n                                        node to an existing workflow).\n  --folder-id <folderId>                Folder to place the new workflow in (`folderId` from Active\n                                        Tab Meta); omit for base root.\n  -b, --base-id <baseId>                Target base ID (required, e.g. bseXXXX). Set default:\n                                        `teable config base-id <id>`.\n  --token <token>                       Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>                 Teable API server base URL\n  --pretty                              Indent JSON for human readability (default: false)\n  -h, --help                            display help for command\n"
        },
        {
          "name": "test-node",
          "summary": "Test automation trigger or action node",
          "description": "Test a specific node (trigger or action) in an automation workflow.\n\n🧪 Testing is ESSENTIAL for building reliable automations!\n\n📋 How to use:\n1. After configuring a trigger, test it to capture sample output\n2. After adding an action, test it with the trigger output\n3. Review the output to verify it works correctly\n4. If test fails, analyze the error and fix the configuration\n\n💡 Tips:\n- Test trigger first to get sample data\n- Test actions in order (each depends on previous outputs)\n- Use recordId to pin a specific row for record-based triggers\n- scheduledTime triggers auto-synthesize timestamps (no input needed)\n- Review output structure to understand available data for next steps\n\n⚠️  Risk considerations:\n- Testing with sideEffect=true WILL execute real actions (send emails, API calls)\n- Default sideEffect=false is safe for testing\n- For production data, consider using askForConfirmation first\n\nParameters:\n- workflowId: The automation workflow ID\n- nodeId: The specific node to test (trigger ID or action ID)\n- recordId: Optional record ID for record-based triggers\n- sideEffect: Whether to execute with real side effects (default: false)\n- withDependency: Whether to test dependency nodes together (default: false)\n\nReturns:\n- success: Whether the test succeeded\n- output: The output data from the node (use this for subsequent actions)\n- error: Error message if test failed",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--node-id <nodeId>",
              "description": "The ID of the node (trigger or action) to test",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--record-id <recordId>",
              "description": "Record ID to use for testing (for record-based triggers)"
            },
            {
              "flags": "--side-effect",
              "description": "Whether to execute with side effects (default: false for safe testing)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--with-dependency",
              "description": "Whether to test with dependency nodes (default: false)"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the workflow",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation test-node [options]\n\nTest a specific node (trigger or action) in an automation workflow.\n\n🧪 Testing is ESSENTIAL for building reliable automations!\n\n📋 How to use:\n1. After configuring a trigger, test it to capture sample output\n2. After adding an action, test it with the trigger output\n3. Review the output to verify it works correctly\n4. If test fails, analyze the error and fix the configuration\n\n💡 Tips:\n- Test trigger first to get sample data\n- Test actions in order (each depends on previous outputs)\n- Use recordId to pin a specific row for record-based triggers\n- scheduledTime triggers auto-synthesize timestamps (no input needed)\n- Review output structure to understand available data for next steps\n\n⚠️  Risk considerations:\n- Testing with sideEffect=true WILL execute real actions (send emails, API calls)\n- Default sideEffect=false is safe for testing\n- For production data, consider using askForConfirmation first\n\nParameters:\n- workflowId: The automation workflow ID\n- nodeId: The specific node to test (trigger ID or action ID)\n- recordId: Optional record ID for record-based triggers\n- sideEffect: Whether to execute with real side effects (default: false)\n- withDependency: Whether to test dependency nodes together (default: false)\n\nReturns:\n- success: Whether the test succeeded\n- output: The output data from the node (use this for subsequent actions)\n- error: Error message if test failed\n\nOptions:\n  --workflow-id <workflowId>  The ID of the workflow\n  --node-id <nodeId>          The ID of the node (trigger or action) to test\n  --record-id <recordId>      Record ID to use for testing (for record-based triggers)\n  --side-effect               Whether to execute with side effects (default: false for safe testing)\n  --with-dependency           Whether to test with dependency nodes (default: false)\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update an automation workflow's name or description",
          "description": "Update an automation workflow's name and/or description.\n\nParameters:\n  - workflowId (required): ID of the automation workflow to update\n  - name (optional): new workflow name\n  - description (optional): new workflow description\n  At least one of name or description must be provided.\n\n✅ Applies immediately — this is a direct edit, not a draft, so no activate-automation call is needed afterward.\nTo change the workflow trigger instead, use setup-automation-trigger.",
          "options": [
            {
              "flags": "--description <description>",
              "description": "The new description for the automation workflow"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--name <name>",
              "description": "The new name for the automation workflow"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--workflow-id <workflowId>",
              "description": "The ID of the automation workflow to update",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable automation update [options]\n\nUpdate an automation workflow's name and/or description.\n\nParameters:\n  - workflowId (required): ID of the automation workflow to update\n  - name (optional): new workflow name\n  - description (optional): new workflow description\n  At least one of name or description must be provided.\n\n✅ Applies immediately — this is a direct edit, not a draft, so no activate-automation call is needed\nafterward.\nTo change the workflow trigger instead, use setup-automation-trigger.\n\nOptions:\n  --workflow-id <workflowId>   The ID of the automation workflow to update\n  --name <name>                The new name for the automation workflow\n  --description <description>  The new description for the automation workflow\n  -b, --base-id <baseId>       Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                               base-id <id>`.\n  --token <token>              Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>        Teable API server base URL\n  --pretty                     Indent JSON for human readability (default: false)\n  -h, --help                   display help for command\n"
        }
      ],
      "help": "Usage: teable automation [options] [command]\n\nautomation operations (list, get, update, activate, setup-trigger, delete, test-node, delete-node,\ngenerate-script, generate-flowchart, get-script-input, get-runs, get-run)\n\nOptions:\n  -h, --help                    display help for command\n\nCommands:\n  list [options]                List all automations in the base\n  get [options]                 Get detailed automation workflow\n  update [options]              Update an automation workflow's name or description\n  activate [options]            Activate(enable draft) or deactivate automation\n  setup-trigger [options]       Setup automation trigger configuration triggers: recordCreated,\n                                recordUpdated, recordMatchesConditions, buttonClick, formSubmitted,\n                                scheduledTime, webhook, emailReceived\n  delete [options]              Delete an automation workflow\n  test-node [options]           Test automation trigger or action node\n  delete-node [options]         Delete an action or logic node from automation workflow\n  generate-script [options]     Generate script action code for automation\n  generate-flowchart [options]  Generate flowchart for automation script action node\n  get-script-input [options]    Get script input from workflow actions\n  get-runs [options]            Get automation run history\n  get-run [options]             Get automation run detail\n  help [command]                display help for command\n\nExamples:\n  teable automation list\n  teable automation get --workflow-id wflXXXX\n  teable automation activate --workflow-id wflXXXX --method activate\n  teable automation get-run --workflow-id wflXXXX --run-id runXXXX\n  teable automation delete --workflow-id wflXXXX\n  teable automation update --workflow-id wflXXXX --name \"New name\"\n"
    },
    {
      "name": "base",
      "description": "base operations (create, get, list, update, delete)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create a base",
          "description": "Creates a new base in the given space.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--icon <icon>",
              "description": "Base icon (emoji)"
            },
            {
              "flags": "--name <name>",
              "description": "Base name"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-s, --space-id <spaceId>",
              "description": "Target space ID (e.g. spcXXXX)",
              "required": true
            }
          ],
          "help": "Usage: teable base create [options]\n\nCreates a new base in the given space.\n\nOptions:\n  -s, --space-id <spaceId>  Target space ID (e.g. spcXXXX)\n  --name <name>             Base name\n  --icon <icon>             Base icon (emoji)\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "delete",
          "summary": "Delete a base",
          "description": "Deletes a base by ID (moves it to trash).",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable base delete [options]\n\nDeletes a base by ID (moves it to trash).\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "get",
          "summary": "Get a base",
          "description": "Fetches metadata for a single base by ID.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable base get [options]\n\nFetches metadata for a single base by ID.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "list",
          "summary": "List bases",
          "description": "Lists bases the current user can access, optionally scoped to one space.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-s, --space-id <spaceId>",
              "description": "List only bases in this space (e.g. spcXXXX)"
            }
          ],
          "help": "Usage: teable base list [options]\n\nLists bases the current user can access, optionally scoped to one space.\n\nOptions:\n  -s, --space-id <spaceId>  List only bases in this space (e.g. spcXXXX)\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "update",
          "summary": "Update a base",
          "description": "Updates a base name and/or icon.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--icon <icon>",
              "description": "New base icon (emoji)"
            },
            {
              "flags": "--name <name>",
              "description": "New base name"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable base update [options]\n\nUpdates a base name and/or icon.\n\nOptions:\n  --name <name>           New base name\n  --icon <icon>           New base icon (emoji)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n",
          "availability": "managed-sandbox-disabled"
        }
      ],
      "help": "Usage: teable base [options] [command]\n\nbase operations (create, get, list, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create a base\n  get [options]     Get a base\n  list [options]    List bases\n  update [options]  Update a base\n  delete [options]  Delete a base\n  help [command]    display help for command\n\nExamples:\n  teable base list\n  teable base list --space-id spcXXXX\n  teable base create --space-id spcXXXX --name \"My Base\"\n  teable base get --base-id bseXXXX\n  teable base update --base-id bseXXXX --name \"Renamed\"\n  teable base delete --base-id bseXXXX\n",
      "availability": "managed-sandbox-disabled"
    },
    {
      "name": "call-api",
      "summary": "Call any Teable API (axios-style: method, url, params, data)",
      "description": "Call any Teable API with axios-style parameters.\n\nIMPORTANT: Use search-api first to find the correct method and URL path.\n\nParameters:\n  - method (required): HTTP method (e.g., \"GET\", \"POST\")\n  - url (required): URL path template from search-api (e.g., \"/table/{tableId}/record\")\n  - params: Merged path + query parameters — path params are split based on the API specification\n  - data: Request body for POST/PUT/PATCH\n\nExample workflow:\n  1. search-api({ query: \"duplicate record\" })\n     → Returns: method: \"POST\", url: \"/table/{tableId}/record/{recordId}/duplicate\"\n  2. call-api({\n       method: \"POST\",\n       url: \"/table/{tableId}/record/{recordId}/duplicate\",\n       params: { tableId: \"tblXXX\", recordId: \"recXXX\" }\n     })",
      "options": [
        {
          "flags": "--data <json>",
          "description": "Request body for POST/PUT/PATCH requests"
        },
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--method <method>",
          "description": "HTTP method (e.g., \"GET\", \"POST\")",
          "required": true
        },
        {
          "flags": "--params <jsonObject>",
          "description": "Merged path + query parameters. Path params are split from query params based on the API specification (e.g., { tableId: \"tblXXX\", take: 10 })"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "--url <url>",
          "description": "URL path template from search-api results (e.g., \"/table/{tableId}/record\")",
          "required": true
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable call-api [options]\n\nCall any Teable API with axios-style parameters.\n\nIMPORTANT: Use search-api first to find the correct method and URL path.\n\nParameters:\n  - method (required): HTTP method (e.g., \"GET\", \"POST\")\n  - url (required): URL path template from search-api (e.g., \"/table/{tableId}/record\")\n  - params: Merged path + query parameters — path params are split based on the API specification\n  - data: Request body for POST/PUT/PATCH\n\nExample workflow:\n  1. search-api({ query: \"duplicate record\" })\n     → Returns: method: \"POST\", url: \"/table/{tableId}/record/{recordId}/duplicate\"\n  2. call-api({\n       method: \"POST\",\n       url: \"/table/{tableId}/record/{recordId}/duplicate\",\n       params: { tableId: \"tblXXX\", recordId: \"recXXX\" }\n     })\n\nOptions:\n  --method <method>       HTTP method (e.g., \"GET\", \"POST\")\n  --url <url>             URL path template from search-api results (e.g.,\n                          \"/table/{tableId}/record\")\n  --params <jsonObject>   Merged path + query parameters. Path params are split from query params\n                          based on the API specification (e.g., { tableId: \"tblXXX\", take: 10 })\n  --data <json>           Request body for POST/PUT/PATCH requests\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "config",
      "description": "Manage project-level CLI configuration",
      "subcommands": [
        {
          "name": "base-id",
          "description": "Save a default base ID into the project-level config file",
          "help": "Usage: teable config base-id [options] <baseId>\n\nSave a default base ID into the project-level config file\n\nOptions:\n  -h, --help  display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "show",
          "description": "Show the resolved CLI configuration (merged user + project)",
          "help": "Usage: teable config show [options]\n\nShow the resolved CLI configuration (merged user + project)\n\nOptions:\n  -h, --help  display help for command\n",
          "availability": "managed-sandbox-disabled"
        }
      ],
      "help": "Usage: teable config [options] [command]\n\nManage project-level CLI configuration\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  base-id <baseId>  Save a default base ID into the project-level config file\n  show              Show the resolved CLI configuration (merged user + project)\n  help [command]    display help for command\n",
      "availability": "managed-sandbox-disabled"
    },
    {
      "name": "env",
      "description": "env operations (list, set, update, delete)",
      "subcommands": [
        {
          "name": "delete",
          "summary": "Delete env variable by id",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--id <id>",
              "description": "Env variable id (evvXXXX)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            }
          ],
          "help": "Usage: teable env delete [options]\n\nOptions:\n  --id <id>              Env variable id (evvXXXX)\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n"
        },
        {
          "name": "list",
          "summary": "List env variables in a namespace",
          "description": "List env variables for USER / APP / AUTOMATION namespace.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--scope-id <scopeId>",
              "description": "App id (appXXXX) for --scope app, or workflow id (wflXXXX) for --scope automation"
            },
            {
              "flags": "--scope-type <scope>",
              "description": "Alias for --scope"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-S, --scope <scope>",
              "description": "One of: user, app, automation",
              "required": true
            }
          ],
          "help": "Usage: teable env list [options]\n\nList env variables for USER / APP / AUTOMATION namespace.\n\nOptions:\n  -S, --scope <scope>    One of: user, app, automation\n  --scope-type <scope>   Alias for --scope\n  --scope-id <scopeId>   App id (appXXXX) for --scope app, or workflow id (wflXXXX) for --scope\n                         automation\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n"
        },
        {
          "name": "set",
          "summary": "Create or update an env variable (upsert by key)",
          "description": "Create the env if it does not exist, otherwise replace its value.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--scope-id <scopeId>",
              "description": "App id for --scope app, or workflow id for --scope automation"
            },
            {
              "flags": "--scope-type <scope>",
              "description": "Alias for --scope"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-S, --scope <scope>",
              "description": "One of: user, app, automation",
              "required": true
            },
            {
              "flags": "-d, --description <text>",
              "description": "Optional description"
            },
            {
              "flags": "-k, --key <key>",
              "description": "Env KEY, ^[A-Z][A-Z0-9_]{0,63}$",
              "required": true
            },
            {
              "flags": "-v, --value <value>",
              "description": "Env value (plaintext)",
              "required": true
            }
          ],
          "help": "Usage: teable env set [options]\n\nCreate the env if it does not exist, otherwise replace its value.\n\nOptions:\n  -S, --scope <scope>       One of: user, app, automation\n  --scope-type <scope>      Alias for --scope\n  --scope-id <scopeId>      App id for --scope app, or workflow id for --scope automation\n  -k, --key <key>           Env KEY, ^[A-Z][A-Z0-9_]{0,63}$\n  -v, --value <value>       Env value (plaintext)\n  -d, --description <text>  Optional description\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update env variable value / description by id",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--id <id>",
              "description": "Env variable id (evvXXXX)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-d, --description <text>",
              "description": "Replace description"
            },
            {
              "flags": "-v, --value <value>",
              "description": "Replace value (plaintext)"
            }
          ],
          "help": "Usage: teable env update [options]\n\nOptions:\n  --id <id>                 Env variable id (evvXXXX)\n  -v, --value <value>       Replace value (plaintext)\n  -d, --description <text>  Replace description\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        }
      ],
      "help": "Usage: teable env [options] [command]\n\nenv operations (list, set, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  list [options]    List env variables in a namespace\n  set [options]     Create or update an env variable (upsert by key)\n  update [options]  Update env variable value / description by id\n  delete [options]  Delete env variable by id\n  help [command]    display help for command\n\nExamples:\n  teable env list --scope user\n  teable env list --scope app --scope-id appXXXX\n  teable env set --scope user --key OPENAI_KEY --value sk-xxx\n  teable env set --scope automation --scope-id wflXXXX --key STRIPE_KEY --value sk_test_...\n  teable env update --id evvXXXX --value new-value\n  teable env delete --id evvXXXX\n"
    },
    {
      "name": "field",
      "description": "field operations (create, get, update, delete)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create a new field in a table",
          "description": "Create a field in a table. Fields are like columns in a spreadsheet.\n\nSMART DEFAULTS:\n  - Type inference: omit \"type\" and it will be inferred from options\n  - Auto-sanitization: select choices get auto-assigned colors\n  - Name resolution: use table/field NAMES instead of IDs where possible\n\nTYPE ALIASES: text, long, num, sel, multi, check, rate, date, user, file, link, rollup, formula, condRollup\n\nBASIC FIELDS: text/long/num/date/check/rate/sel/multi/user/file\n\nLINK - Connect tables\n  options: { foreignTableName: \"Projects\" } or { foreignTableId: \"tblXXX\" }\n\nLOOKUP - Display data from linked tables\n  isLookup: true + lookupOptions: { linkFieldName: \"Project\", lookupFieldName: \"Status\" }\n  (or use IDs: linkFieldId, lookupFieldId)\n\nROLLUP - Aggregate values from linked records\n  lookupOptions: { linkFieldName: \"Tasks\", lookupFieldName: \"Hours\" }\n  options: { expression: \"SUM({values})\" }\n\nFORMULA - Calculate values (field names auto-converted to IDs)\n  options: { expression: \"{Budget} - {Actual}\" }\n\nCONDITIONAL LOOKUP - Query ANY table with filters (no link needed)\n  isLookup + isConditionalLookup + lookupOptions: { foreignTableName, filter, lookupFieldName }\n\nOUTPUT: { field: \"fldXXX:Name:type\", hint: \"...\" }",
          "options": [
            {
              "flags": "--ai-config <jsonObject>",
              "description": "AI field configuration (e.g., { type: \"summary\", sourceFieldName: \"Description\" })"
            },
            {
              "flags": "--description <description>",
              "description": "Field description"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--is-conditional-lookup",
              "description": "Whether lookup field is a conditional lookup field"
            },
            {
              "flags": "--is-lookup",
              "description": "Whether the field is a lookup field"
            },
            {
              "flags": "--lookup-options <jsonObject>",
              "description": "Lookup options"
            },
            {
              "flags": "--name <name>",
              "description": "Field name",
              "required": true
            },
            {
              "flags": "--options <jsonObject>",
              "description": "Field-specific options"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "Table ID to create field in",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--type <fieldType>",
              "description": "Field type: text/num/sel/multi/check/rate/date/user/file/link/rollup/formula/condRollup"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable field create [options]\n\nCreate a field in a table. Fields are like columns in a spreadsheet.\n\nSMART DEFAULTS:\n  - Type inference: omit \"type\" and it will be inferred from options\n  - Auto-sanitization: select choices get auto-assigned colors\n  - Name resolution: use table/field NAMES instead of IDs where possible\n\nTYPE ALIASES: text, long, num, sel, multi, check, rate, date, user, file, link, rollup, formula,\ncondRollup\n\nBASIC FIELDS: text/long/num/date/check/rate/sel/multi/user/file\n\nLINK - Connect tables\n  options: { foreignTableName: \"Projects\" } or { foreignTableId: \"tblXXX\" }\n\nLOOKUP - Display data from linked tables\n  isLookup: true + lookupOptions: { linkFieldName: \"Project\", lookupFieldName: \"Status\" }\n  (or use IDs: linkFieldId, lookupFieldId)\n\nROLLUP - Aggregate values from linked records\n  lookupOptions: { linkFieldName: \"Tasks\", lookupFieldName: \"Hours\" }\n  options: { expression: \"SUM({values})\" }\n\nFORMULA - Calculate values (field names auto-converted to IDs)\n  options: { expression: \"{Budget} - {Actual}\" }\n\nCONDITIONAL LOOKUP - Query ANY table with filters (no link needed)\n  isLookup + isConditionalLookup + lookupOptions: { foreignTableName, filter, lookupFieldName }\n\nOUTPUT: { field: \"fldXXX:Name:type\", hint: \"...\" }\n\nOptions:\n  --table-id <tableId>           Table ID to create field in\n  --name <name>                  Field name\n  --type <fieldType>             Field type:\n                                 text/num/sel/multi/check/rate/date/user/file/link/rollup/formula/condRollup\n  --description <description>    Field description\n  --options <jsonObject>         Field-specific options\n  --is-lookup                    Whether the field is a lookup field\n  --is-conditional-lookup        Whether lookup field is a conditional lookup field\n  --lookup-options <jsonObject>  Lookup options\n  --ai-config <jsonObject>       AI field configuration (e.g., { type: \"summary\", sourceFieldName:\n                                 \"Description\" })\n  -b, --base-id <baseId>         Target base ID (required, e.g. bseXXXX). Set default: `teable\n                                 config base-id <id>`.\n  --token <token>                Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>          Teable API server base URL\n  --pretty                       Indent JSON for human readability (default: false)\n  -h, --help                     display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete a field permanently",
          "description": "Delete a field permanently (removes field and all its data).\n\nParameters:\n  - tableId (required): The table ID\n  - fieldId (required): The field ID to delete\n\n⚠️ Warning: Permanent operation. Cannot delete primary field or last field.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--field-id <fieldId>",
              "description": "The ID of the field to delete",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table containing the field",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable field delete [options]\n\nDelete a field permanently (removes field and all its data).\n\nParameters:\n  - tableId (required): The table ID\n  - fieldId (required): The field ID to delete\n\n⚠️ Warning: Permanent operation. Cannot delete primary field or last field.\n\nOptions:\n  --table-id <tableId>    The ID of the table containing the field\n  --field-id <fieldId>    The ID of the field to delete\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "get",
          "summary": "Get fields metadata from a table",
          "description": "Get fields metadata from a table.\n\nParameters:\n  - tableId (required): The table ID\n  - fieldId (optional): Filter by specific field ID\n  - fieldName (optional): Filter by specific field name\n\nReturns field information:\n  - id: Unique field identifier\n  - name: Display name\n  - type: Field type (text, number, date, etc.)\n  - description: Field description\n  - dbFieldName: Database column name\n  - options: Field configuration",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--field-id <fieldId>",
              "description": "Optional: filter by specific field ID"
            },
            {
              "flags": "--field-name <fieldName>",
              "description": "Optional: filter by specific field name"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to get fields from",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable field get [options]\n\nGet fields metadata from a table.\n\nParameters:\n  - tableId (required): The table ID\n  - fieldId (optional): Filter by specific field ID\n  - fieldName (optional): Filter by specific field name\n\nReturns field information:\n  - id: Unique field identifier\n  - name: Display name\n  - type: Field type (text, number, date, etc.)\n  - description: Field description\n  - dbFieldName: Database column name\n  - options: Field configuration\n\nOptions:\n  --table-id <tableId>      The ID of the table to get fields from\n  --field-id <fieldId>      Optional: filter by specific field ID\n  --field-name <fieldName>  Optional: filter by specific field name\n  -b, --base-id <baseId>    Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                            base-id <id>`.\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update field properties or type",
          "description": "Update field properties with partial updates or type conversion.\n\n📋 SYNTAX:\n• Partial: { updates: { name: \"New Name\" } }\n• Dot notation: { updates: { \"options.formatting.precision\": 2 } }\n• ShowAs: { updates: { \"options.showAs\": { type: \"ring\", maxValue: 100 } } }\n• Type change: { updates: { type: \"number\", options: {...} } }\n\n✅ Type aliases: \"text\"→singleLineText, \"long\"→longText, \"sel\"→singleSelect, \"multi\"→multipleSelect\n✅ Formula expressions can use field NAMES - auto-converted to IDs!\n✅ Name resolution: linkFieldName, lookupFieldName → auto-resolved\n\n⚠️ FORMATTING:\n• Currency: { type: \"currency\", symbol: \"$\", precision: 2 } (use \"symbol\", NOT \"currency\")\n• Date time: { date: \"YYYY-MM-DD\", time: \"HH:mm\" | \"hh:mm A\" | \"None\" }\n• To remove optional property: omit it from updates (null not supported)",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--field-id <fieldId>",
              "description": "The ID of the field to update",
              "required": true
            },
            {
              "flags": "--name <name>",
              "description": "Convenience flag: set field name (shortcut for --updates '{\"name\":\"X\"}')"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table containing the field",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--updates <jsonObject>",
              "description": "Field properties to update as an object. Can include: name, description, type, options, isLookup, lookupOptions, etc. Supports nested properties using dot notation (e.g., {\"options.formatting.type\": \"currency\"})"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable field update [options]\n\nUpdate field properties with partial updates or type conversion.\n\n📋 SYNTAX:\n• Partial: { updates: { name: \"New Name\" } }\n• Dot notation: { updates: { \"options.formatting.precision\": 2 } }\n• ShowAs: { updates: { \"options.showAs\": { type: \"ring\", maxValue: 100 } } }\n• Type change: { updates: { type: \"number\", options: {...} } }\n\n✅ Type aliases: \"text\"→singleLineText, \"long\"→longText, \"sel\"→singleSelect, \"multi\"→multipleSelect\n✅ Formula expressions can use field NAMES - auto-converted to IDs!\n✅ Name resolution: linkFieldName, lookupFieldName → auto-resolved\n\n⚠️ FORMATTING:\n• Currency: { type: \"currency\", symbol: \"$\", precision: 2 } (use \"symbol\", NOT \"currency\")\n• Date time: { date: \"YYYY-MM-DD\", time: \"HH:mm\" | \"hh:mm A\" | \"None\" }\n• To remove optional property: omit it from updates (null not supported)\n\nOptions:\n  --table-id <tableId>    The ID of the table containing the field\n  --field-id <fieldId>    The ID of the field to update\n  --updates <jsonObject>  Field properties to update as an object. Can include: name, description,\n                          type, options, isLookup, lookupOptions, etc. Supports nested properties\n                          using dot notation (e.g., {\"options.formatting.type\": \"currency\"})\n  --name <name>           Convenience flag: set field name (shortcut for --updates '{\"name\":\"X\"}')\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        }
      ],
      "help": "Usage: teable field [options] [command]\n\nfield operations (create, get, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create a new field in a table\n  get [options]     Get fields metadata from a table\n  update [options]  Update field properties or type\n  delete [options]  Delete a field permanently\n  help [command]    display help for command\n\nExamples:\n  teable field get --table-id tblXXXX\n  teable field create --table-id tblXXXX --name Email --type email\n  teable field delete --table-id tblXXXX --field-id fldXXXX\n"
    },
    {
      "name": "folder",
      "description": "folder operations (create, update, delete, move)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create a new folder in a base",
          "description": "Creates a new folder node in the specified base.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--name <name>",
              "description": "Name of the folder to create",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable folder create [options]\n\nCreates a new folder node in the specified base.\n\nOptions:\n  --name <name>           Name of the folder to create\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete a folder in a base (children move to parent)",
          "description": "Deletes a folder node from the specified base. Children of the folder are moved to the parent level.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "ID of the folder to delete",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable folder delete [options]\n\nDeletes a folder node from the specified base. Children of the folder are moved to the parent level.\n\nOptions:\n  --folder-id <folderId>  ID of the folder to delete\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "move",
          "summary": "Move a node to a different parent or position",
          "description": "Moves a node (table, app, automation, etc.) to a different parent folder or reorders it within the same level.",
          "options": [
            {
              "flags": "--anchor-id <anchorId>",
              "description": "ID of the sibling node to position relative to"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--node-id <nodeId>",
              "description": "ID of the node to move",
              "required": true
            },
            {
              "flags": "--parent-id <parentId>",
              "description": "Target parent folder ID (null to move to root)"
            },
            {
              "flags": "--position <position>",
              "description": "Position relative to anchor",
              "choices": [
                "before",
                "after"
              ]
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable folder move [options]\n\nMoves a node (table, app, automation, etc.) to a different parent folder or reorders it within the\nsame level.\n\nOptions:\n  --node-id <nodeId>      ID of the node to move\n  --parent-id <parentId>  Target parent folder ID (null to move to root)\n  --anchor-id <anchorId>  ID of the sibling node to position relative to\n  --position <position>   Position relative to anchor (choices: \"before\", \"after\")\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update a folder in a base",
          "description": "Updates an existing folder node in the specified base.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "ID of the folder to update",
              "required": true
            },
            {
              "flags": "--name <name>",
              "description": "New name for the folder",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable folder update [options]\n\nUpdates an existing folder node in the specified base.\n\nOptions:\n  --folder-id <folderId>  ID of the folder to update\n  --name <name>           New name for the folder\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        }
      ],
      "help": "Usage: teable folder [options] [command]\n\nfolder operations (create, update, delete, move)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create a new folder in a base\n  update [options]  Update a folder in a base\n  delete [options]  Delete a folder in a base (children move to parent)\n  move [options]    Move a node to a different parent or position\n  help [command]    display help for command\n\nExamples:\n  teable folder create --name \"Project Alpha\"\n  teable folder update --folder-id fldXXXX --name \"Project Beta\"\n  teable folder delete --folder-id fldXXXX\n"
    },
    {
      "name": "get-ai-config",
      "summary": "Get AI field configuration documentation with available models",
      "description": "Shortcut for: get-doc --topic field.ai\n\nReturns the AI-enhanced field configuration documentation with dynamically\nresolved available models for the current base. The doc includes:\n  - Available text models (for text/select/rating/date fields)\n  - Available image models (for attachment fields)\n  - AI field types: customization, extraction, classification, tag,\n    summary, translation, improvement, rating, imageGeneration, etc.\n  - aiConfig schema and usage examples",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable get-ai-config [options]\n\nShortcut for: get-doc --topic field.ai\n\nReturns the AI-enhanced field configuration documentation with dynamically\nresolved available models for the current base. The doc includes:\n  - Available text models (for text/select/rating/date fields)\n  - Available image models (for attachment fields)\n  - AI field types: customization, extraction, classification, tag,\n    summary, translation, improvement, rating, imageGeneration, etc.\n  - aiConfig schema and usage examples\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "get-collaborators",
      "summary": "Get collaborators in the current base with pagination and search",
      "description": "Get the list of collaborators in the current base with pagination and search.\n\nParameters:\n  - search - Filter by name or email (optional)\n  - skip - Number to skip for pagination (optional)\n  - take - Number to return, default 20, max 100 (optional)\n  - includeSystem - Include system users (optional)\n\nReturns:\n  - users: Array of { id, name, email, avatar }\n  - total: Total number of collaborators matching the query\n\nExamples:\n  All: {}\n  Search: { search: \"john\" }\n  Pagination: { skip: 20, take: 20 }",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--include-system",
          "description": "Whether to include system users (default: false)"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--search <keyword>",
          "description": "Search keyword to filter collaborators by name or email"
        },
        {
          "flags": "--skip <number>",
          "description": "Number of collaborators to skip for pagination"
        },
        {
          "flags": "--take <number>",
          "description": "Number of collaborators to return (default: 20, max: 100)"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable get-collaborators [options]\n\nGet the list of collaborators in the current base with pagination and search.\n\nParameters:\n  - search - Filter by name or email (optional)\n  - skip - Number to skip for pagination (optional)\n  - take - Number to return, default 20, max 100 (optional)\n  - includeSystem - Include system users (optional)\n\nReturns:\n  - users: Array of { id, name, email, avatar }\n  - total: Total number of collaborators matching the query\n\nExamples:\n  All: {}\n  Search: { search: \"john\" }\n  Pagination: { skip: 20, take: 20 }\n\nOptions:\n  --search <keyword>      Search keyword to filter collaborators by name or email\n  --skip <number>         Number of collaborators to skip for pagination\n  --take <number>         Number of collaborators to return (default: 20, max: 100)\n  --include-system        Whether to include system users (default: false)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "get-doc",
      "summary": "Get documentation for a topic",
      "description": "Get comprehensive documentation for Teable features and configurations.\nUse this tool to understand configuration formats before creating or updating resources.\nWorkflow:\n1. Identify the category needed (field, view, or record)\n2. Query the specific subtopic (e.g., \"field.formula\" or \"field.colors\")\n3. Use examples from documentation to construct correct configurations",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "--topic <topic>",
          "description": "Documentation topic in format \"category.subtopic\":\n\nField Configuration:\n- field.basic: Basic fields (text, number, date, checkbox, user, attachment, rating)\n- field.select: Select fields (single/multiple select with choices)\n- field.link: Link fields (relationships between tables)\n- field.formula: Formula fields (computed values with expressions)\n- field.lookup: Lookup fields (display values from linked records, includes conditional lookup)\n- field.rollup: Rollup fields (aggregate values from linked records, includes conditional rollup)\n- field.ai: AI-enhanced fields (extraction, classification, etc.)\n- field.formatting: Number and datetime formatting options\n- field.show-as: Visual representation options (progress bars, rings, charts, clickable links)\n- field.colors: Available color options for fields\n\nView Configuration:\n- view.filter: Filter records by conditions\n- view.sort: Sort records by field values\n- view.group: Group records into collapsible sections\n- view.column: Configure visible columns and widths\n\nRecord Data:\n- record.value-format: Exact format for field values when creating/updating records without typecasting\n\nAutomation:\n- automation.trigger: Trigger types and configuration (recordCreated, recordUpdated, recordMatchesConditions, formSubmitted, scheduledTime, buttonClick, webhook)\n- automation.send-email: SendEmail action configuration with system or custom SMTP\n- automation.api: Teable REST API reference for scripts (records, fields, filters)\n- automation.ai: Call AI from a script (prompt, attachments, structured output)\n\nExternal Integrations (for automation scripts):\n- integration.slack: Slack API - send messages, notifications, interact with channels and users\n\nScrape:\n- scrape.datasets: Available scrape datasets and input requirements\n\nApp Builder:\n- app.ai: How the app calls AI models through the Teable proxy (env vars, server-side usage, available model keys)\n\nReference:\n- reference.url: Teable resource URL patterns and ID prefixes — parse a pasted link into its resource type and IDs, or generate a link to a table/record/automation/app/dashboard",
          "required": true
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable get-doc [options]\n\nGet comprehensive documentation for Teable features and configurations.\nUse this tool to understand configuration formats before creating or updating resources.\nWorkflow:\n1. Identify the category needed (field, view, or record)\n2. Query the specific subtopic (e.g., \"field.formula\" or \"field.colors\")\n3. Use examples from documentation to construct correct configurations\n\nOptions:\n  --topic <topic>         Documentation topic in format \"category.subtopic\":\n                          \n                          Field Configuration:\n                          - field.basic: Basic fields (text, number, date, checkbox, user,\n                          attachment, rating)\n                          - field.select: Select fields (single/multiple select with choices)\n                          - field.link: Link fields (relationships between tables)\n                          - field.formula: Formula fields (computed values with expressions)\n                          - field.lookup: Lookup fields (display values from linked records,\n                          includes conditional lookup)\n                          - field.rollup: Rollup fields (aggregate values from linked records,\n                          includes conditional rollup)\n                          - field.ai: AI-enhanced fields (extraction, classification, etc.)\n                          - field.formatting: Number and datetime formatting options\n                          - field.show-as: Visual representation options (progress bars, rings,\n                          charts, clickable links)\n                          - field.colors: Available color options for fields\n                          \n                          View Configuration:\n                          - view.filter: Filter records by conditions\n                          - view.sort: Sort records by field values\n                          - view.group: Group records into collapsible sections\n                          - view.column: Configure visible columns and widths\n                          \n                          Record Data:\n                          - record.value-format: Exact format for field values when\n                          creating/updating records without typecasting\n                          \n                          Automation:\n                          - automation.trigger: Trigger types and configuration (recordCreated,\n                          recordUpdated, recordMatchesConditions, formSubmitted, scheduledTime,\n                          buttonClick, webhook)\n                          - automation.send-email: SendEmail action configuration with system or\n                          custom SMTP\n                          - automation.api: Teable REST API reference for scripts (records, fields,\n                          filters)\n                          - automation.ai: Call AI from a script (prompt, attachments, structured\n                          output)\n                          \n                          External Integrations (for automation scripts):\n                          - integration.slack: Slack API - send messages, notifications, interact\n                          with channels and users\n                          \n                          Scrape:\n                          - scrape.datasets: Available scrape datasets and input requirements\n                          \n                          App Builder:\n                          - app.ai: How the app calls AI models through the Teable proxy (env vars,\n                          server-side usage, available model keys)\n                          \n                          Reference:\n                          - reference.url: Teable resource URL patterns and ID prefixes — parse a\n                          pasted link into its resource type and IDs, or generate a link to a\n                          table/record/automation/app/dashboard\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "get-node-tree",
      "summary": "Get the tree structure of all nodes (tables, automations, apps, etc.) in a base",
      "description": "Retrieves the full tree structure of nodes in a base, including folders, tables, apps, and workflows with their hierarchy.",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable get-node-tree [options]\n\nRetrieves the full tree structure of nodes in a base, including folders, tables, apps, and workflows\nwith their hierarchy.\n\nOptions:\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "import",
      "summary": "Import tabular data into Teable (analyzes by default; --table-name creates, --table-id appends)",
      "description": "Import tabular data (CSV/Excel/TSV/inline JSON) into Teable.\n\nBehavior — selected by which target flag is set:\n  no --table-name AND no --table-id → analyze: returns sheets, columns, attachmentToken\n  --table-name <name>               → create new table (fields + types inferred from data)\n  --table-id <id>                   → append rows (never updates or upserts)\n\nInput source (at most one explicit flag; otherwise stdin auto-detected):\n  --file <path> | --attachment-token <token> | --data <json|csv>\n\nExamples:\n  # Analyze (no target flag) — returns sheets, columns, attachmentToken\n  teable import --file data.xlsx\n\n  # Create new table from a file\n  teable import --file data.csv --table-name \"Users\"\n\n  # Append via stdin — mapping auto-inferred when headers match fields\n  echo '[{\"Title\":\"Bug 1\"}]' | teable import --table-id tblXXX\n\n  # Explicit mapping when headers do NOT match (keys = field name or fldXXX)\n  teable import --file legacy.csv --table-id tblXXX --mappings '{\"Title\":0,\"Budget\":2}'\n\n  # Reuse attachmentToken from a prior analyze (pick a specific sheet)\n  teable import --attachment-token <t> --table-name \"Data\" --sheet \"Sheet2\"",
      "options": [
        {
          "flags": "--attachment-token <token>",
          "description": "Already-uploaded file token"
        },
        {
          "flags": "--data <content>",
          "description": "Inline data: JSON array of objects (auto-converted to CSV) or raw CSV text"
        },
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--file <path>",
          "description": "Local CSV/Excel/TSV file (auto-uploads)"
        },
        {
          "flags": "--folder-id <id>",
          "description": "Place the created table under this base-node folder (only with --table-name)"
        },
        {
          "flags": "--mappings <json>",
          "description": "Column→field mapping. Optional — omit to auto-match when source headers equal target field names (case-insensitive fallback; unambiguous only). New table: [{\"column\":\"<src>\",\"field\":\"<target>\",\"type\":\"<type>\"}, ...]. Existing table: {\"<field name or fldXXX>\": <0-based source column index | null>, ...}. Pass null to skip a field. Field types: text | long | number | checkbox | date | singleSelect | multiSelect | rating"
        },
        {
          "flags": "--no-header",
          "description": "First row is data, not a header"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--sheet <name>",
          "description": "Excel worksheet: pass the sheet name (e.g. \"Sheet2\") or the key from a prior analysis call (e.g. \"sheet1\"). Default: first sheet"
        },
        {
          "flags": "--table-id <id>",
          "description": "Append data to an existing table"
        },
        {
          "flags": "--table-name <name>",
          "description": "Create a new table with this name"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable import [options]\n\nImport tabular data (CSV/Excel/TSV/inline JSON) into Teable.\n\nBehavior — selected by which target flag is set:\n  no --table-name AND no --table-id → analyze: returns sheets, columns, attachmentToken\n  --table-name <name>               → create new table (fields + types inferred from data)\n  --table-id <id>                   → append rows (never updates or upserts)\n\nInput source (at most one explicit flag; otherwise stdin auto-detected):\n  --file <path> | --attachment-token <token> | --data <json|csv>\n\nExamples:\n  # Analyze (no target flag) — returns sheets, columns, attachmentToken\n  teable import --file data.xlsx\n\n  # Create new table from a file\n  teable import --file data.csv --table-name \"Users\"\n\n  # Append via stdin — mapping auto-inferred when headers match fields\n  echo '[{\"Title\":\"Bug 1\"}]' | teable import --table-id tblXXX\n\n  # Explicit mapping when headers do NOT match (keys = field name or fldXXX)\n  teable import --file legacy.csv --table-id tblXXX --mappings '{\"Title\":0,\"Budget\":2}'\n\n  # Reuse attachmentToken from a prior analyze (pick a specific sheet)\n  teable import --attachment-token <t> --table-name \"Data\" --sheet \"Sheet2\"\n\nOptions:\n  --file <path>               Local CSV/Excel/TSV file (auto-uploads)\n  --attachment-token <token>  Already-uploaded file token\n  --data <content>            Inline data: JSON array of objects (auto-converted to CSV) or raw CSV\n                              text\n  --table-name <name>         Create a new table with this name\n  --table-id <id>             Append data to an existing table\n  --folder-id <id>            Place the created table under this base-node folder (only with\n                              --table-name)\n  --sheet <name>              Excel worksheet: pass the sheet name (e.g. \"Sheet2\") or the key from a\n                              prior analysis call (e.g. \"sheet1\"). Default: first sheet\n  --no-header                 First row is data, not a header\n  --mappings <json>           Column→field mapping. Optional — omit to auto-match when source\n                              headers equal target field names (case-insensitive fallback;\n                              unambiguous only). New table:\n                              [{\"column\":\"<src>\",\"field\":\"<target>\",\"type\":\"<type>\"}, ...]. Existing\n                              table: {\"<field name or fldXXX>\": <0-based source column index |\n                              null>, ...}. Pass null to skip a field. Field types: text | long |\n                              number | checkbox | date | singleSelect | multiSelect | rating\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
    },
    {
      "name": "import-airtable",
      "summary": "Import an Airtable base into Teable",
      "description": "Import an Airtable base (tables, fields, links, optionally views) into Teable using the native importer. Credential is automatic when Airtable is connected in the connectors menu — the connected integration's token is resolved and refreshed server-side, so no token needs to be pasted. Override with --integration-id <id> or --access-token <pat>.\n\nTypical flow:\n  1. teable import-airtable --analyze                       # list accessible Airtable bases\n  2. teable import-airtable --analyze --airtable-base-id appXXXX   # inspect one base schema\n  3. teable import-airtable --space-id spcXXXX --airtable-base-id appXXXX --base-name \"My Base\"",
      "options": [
        {
          "flags": "--access-token <pat>",
          "description": "Airtable personal access token — overrides the connected integration for this run"
        },
        {
          "flags": "--airtable-base-id <appId>",
          "description": "Airtable base id (appXXXX); omit with --analyze to list"
        },
        {
          "flags": "--analyze",
          "description": "List accessible Airtable bases, or summarize one base schema",
          "default": false
        },
        {
          "flags": "--base-id <baseId>",
          "description": "Import into this existing Teable base instead of creating one"
        },
        {
          "flags": "--base-name <name>",
          "description": "Name for the new Teable base (required unless --base-id is set)"
        },
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--folder-id <id>",
          "description": "Place the imported tables under this base-node folder (only with --base-id)"
        },
        {
          "flags": "--import-view-config",
          "description": "Import view filters/sorts/grouping (requires --share-link)",
          "default": false
        },
        {
          "flags": "--integration-id <id>",
          "description": "Connected Airtable integration id (auto-detected when omitted; token resolved server-side)"
        },
        {
          "flags": "--no-import-attachments",
          "description": "Skip downloading/re-uploading attachments"
        },
        {
          "flags": "--no-import-records",
          "description": "Import structure only, skip record data"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--share-link <url>",
          "description": "Public Airtable shared-base link, for view config import"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-s, --space-id <spaceId>",
          "description": "Target Teable space for a new base (required unless --base-id is set)"
        }
      ],
      "help": "Usage: teable import-airtable [options]\n\nImport an Airtable base (tables, fields, links, optionally views) into Teable using the native\nimporter. Credential is automatic when Airtable is connected in the connectors menu — the connected\nintegration's token is resolved and refreshed server-side, so no token needs to be pasted. Override\nwith --integration-id <id> or --access-token <pat>.\n\nTypical flow:\n  1. teable import-airtable --analyze                       # list accessible Airtable bases\n  2. teable import-airtable --analyze --airtable-base-id appXXXX   # inspect one base schema\n  3. teable import-airtable --space-id spcXXXX --airtable-base-id appXXXX --base-name \"My Base\"\n\nOptions:\n  --analyze                   List accessible Airtable bases, or summarize one base schema (default:\n                              false)\n  -s, --space-id <spaceId>    Target Teable space for a new base (required unless --base-id is set)\n  --airtable-base-id <appId>  Airtable base id (appXXXX); omit with --analyze to list\n  --base-name <name>          Name for the new Teable base (required unless --base-id is set)\n  --base-id <baseId>          Import into this existing Teable base instead of creating one\n  --folder-id <id>            Place the imported tables under this base-node folder (only with\n                              --base-id)\n  --access-token <pat>        Airtable personal access token — overrides the connected integration\n                              for this run\n  --integration-id <id>       Connected Airtable integration id (auto-detected when omitted; token\n                              resolved server-side)\n  --no-import-records         Import structure only, skip record data\n  --no-import-attachments     Skip downloading/re-uploading attachments\n  --import-view-config        Import view filters/sorts/grouping (requires --share-link) (default:\n                              false)\n  --share-link <url>          Public Airtable shared-base link, for view config import\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
    },
    {
      "name": "import-status",
      "summary": "Query import job status for a table",
      "description": "Query the status of an asynchronous import job for a given table ID.",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--poll",
          "description": "Poll until import completes, fails, or times out",
          "default": false
        },
        {
          "flags": "--poll-interval <ms>",
          "description": "Polling interval in milliseconds (minimum 5000)",
          "default": "5000"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--table-id <tableId>",
          "description": "The table ID to check import status for",
          "required": true
        },
        {
          "flags": "--timeout <ms>",
          "description": "Give up after this many ms of polling; exits with status \"timeout\"",
          "default": "1800000"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        }
      ],
      "help": "Usage: teable import-status [options]\n\nQuery the status of an asynchronous import job for a given table ID.\n\nOptions:\n  --table-id <tableId>   The table ID to check import status for\n  --poll                 Poll until import completes, fails, or times out (default: false)\n  --poll-interval <ms>   Polling interval in milliseconds (minimum 5000) (default: \"5000\")\n  --timeout <ms>         Give up after this many ms of polling; exits with status \"timeout\"\n                         (default: \"1800000\")\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n"
    },
    {
      "name": "integration",
      "description": "integration operations (connect, list, get-token)",
      "subcommands": [
        {
          "name": "connect",
          "summary": "Create a connection to an external service",
          "description": "Open integration OAuth authorization page in browser",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--interval <number>",
              "description": "Polling interval in seconds (default: 2)"
            },
            {
              "flags": "--name <name>",
              "description": "The name of the integration",
              "required": true,
              "default": "Slack integration"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--provider <provider>",
              "description": "The integration provider to connect",
              "required": true,
              "choices": [
                "slack"
              ]
            },
            {
              "flags": "--timeout <number>",
              "description": "Wait timeout in seconds (default: 120)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--wait",
              "description": "Wait for OAuth connection completion by polling integration status"
            }
          ],
          "help": "Usage: teable integration connect [options]\n\nOpen integration OAuth authorization page in browser\n\nOptions:\n  --provider <provider>  The integration provider to connect (choices: \"slack\")\n  --name <name>          The name of the integration (default: \"Slack integration\")\n  --wait                 Wait for OAuth connection completion by polling integration status\n  --timeout <number>     Wait timeout in seconds (default: 120)\n  --interval <number>    Polling interval in seconds (default: 2)\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n",
          "availability": "managed-sandbox-disabled"
        },
        {
          "name": "get-token",
          "summary": "Get access token for a connected integration",
          "description": "Get a valid access token for a connected integration (e.g., Slack).\nToken is auto-refreshed if expired.\n\nUse get-user-integrations first to find the integration ID.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--integration-id <integrationId>",
              "description": "The integration ID (from get-user-integrations)",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            }
          ],
          "help": "Usage: teable integration get-token [options]\n\nGet a valid access token for a connected integration (e.g., Slack).\nToken is auto-refreshed if expired.\n\nUse get-user-integrations first to find the integration ID.\n\nOptions:\n  --integration-id <integrationId>  The integration ID (from get-user-integrations)\n  --token <token>                   Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>             Teable API server base URL\n  --pretty                          Indent JSON for human readability (default: false)\n  -h, --help                        display help for command\n"
        },
        {
          "name": "list",
          "summary": "Get user connected integrations",
          "description": "Get the current user's external integrations status (e.g., Slack).\n\nUse this tool to check if user has connected external services before creating automation scripts.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--provider <provider>",
              "description": "Filter by provider, supported values: slack"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            }
          ],
          "help": "Usage: teable integration list [options]\n\nGet the current user's external integrations status (e.g., Slack).\n\nUse this tool to check if user has connected external services before creating automation scripts.\n\nOptions:\n  --provider <provider>  Filter by provider, supported values: slack\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n"
        }
      ],
      "help": "Usage: teable integration [options] [command]\n\nintegration operations (connect, list, get-token)\n\nOptions:\n  -h, --help           display help for command\n\nCommands:\n  connect [options]    Create a connection to an external service\n  list [options]       Get user connected integrations\n  get-token [options]  Get access token for a connected integration\n  help [command]       display help for command\n\nExamples:\n  teable integration list\n  teable integration connect --provider slack --name \"My Slack\"\n"
    },
    {
      "name": "record",
      "description": "record operations (create, get, update, delete)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create new records in a table",
          "description": "Create records using compact array format (saves tokens).\n\nFORMAT:\n{\n  \"tableId\": \"tblXXX\",\n  \"header\": [\"Name\", \"Status\", \"Priority\"],\n  \"records\": [\n    [\"Task A\", \"Done\", \"High\"],\n    [\"Task B\", \"Pending\", \"Low\"]\n  ]\n}\n\nRULES:\n- header: Array of field names (define column order)\n- records: 2D array where each row matches header order\n- Empty cells: use null or \"\"\n\nVALUE TYPES:\n- Text: \"Hello\"\n- Number: 42 or 3.14\n- Boolean: true / false\n- Date: \"2025-01-27\"\n- Select: \"Option A\"\n- MultiSelect: [\"A\", \"B\"]\n- Link: \"Record Name\"",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--header <jsonArray>",
              "description": "Array of field names in order. Example: [\"Name\", \"Status\", \"Priority\"]"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--records <jsonArray>",
              "description": "Array of record rows. Each row is an array of values matching header order. Example: [[\"Task A\", \"Done\", 1], [\"Task B\", \"Pending\", 2]]",
              "required": true
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to create records in",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--typecast",
              "description": "Automatically convert field values to proper types"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable record create [options]\n\nCreate records using compact array format (saves tokens).\n\nFORMAT:\n{\n  \"tableId\": \"tblXXX\",\n  \"header\": [\"Name\", \"Status\", \"Priority\"],\n  \"records\": [\n    [\"Task A\", \"Done\", \"High\"],\n    [\"Task B\", \"Pending\", \"Low\"]\n  ]\n}\n\nRULES:\n- header: Array of field names (define column order)\n- records: 2D array where each row matches header order\n- Empty cells: use null or \"\"\n\nVALUE TYPES:\n- Text: \"Hello\"\n- Number: 42 or 3.14\n- Boolean: true / false\n- Date: \"2025-01-27\"\n- Select: \"Option A\"\n- MultiSelect: [\"A\", \"B\"]\n- Link: \"Record Name\"\n\nOptions:\n  --table-id <tableId>    The ID of the table to create records in\n  --header <jsonArray>    Array of field names in order. Example: [\"Name\", \"Status\", \"Priority\"]\n  --records <jsonArray>   Array of record rows. Each row is an array of values matching header\n                          order. Example: [[\"Task A\", \"Done\", 1], [\"Task B\", \"Pending\", 2]]\n  --typecast              Automatically convert field values to proper types\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete records from a table",
          "description": "Delete records from a table.\n\nParameters:\n  - tableId (required): The table ID\n  - recordIds (required): Array of record IDs to delete (max 1000)\n\nReturns:\n  - success: Whether the operation succeeded\n  - deletedCount: Number of records deleted\n\nExample:\n  { tableId: \"tblXXX\", recordIds: [\"rec001\", \"rec002\"] }",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--record-ids <jsonArray>",
              "description": "Array of record IDs to delete (max 1000)",
              "required": true
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table containing the records",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable record delete [options]\n\nDelete records from a table.\n\nParameters:\n  - tableId (required): The table ID\n  - recordIds (required): Array of record IDs to delete (max 1000)\n\nReturns:\n  - success: Whether the operation succeeded\n  - deletedCount: Number of records deleted\n\nExample:\n  { tableId: \"tblXXX\", recordIds: [\"rec001\", \"rec002\"] }\n\nOptions:\n  --table-id <tableId>      The ID of the table containing the records\n  --record-ids <jsonArray>  Array of record IDs to delete (max 1000)\n  -b, --base-id <baseId>    Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                            base-id <id>`.\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "get",
          "summary": "Query records (--view-id applies filter/sort/group, --search for fuzzy match, --record-id for single record)",
          "description": "Retrieve records from a table in compact tabular format.\n\nOptions:\n  --table-id <id>       (required) Table to query\n  --view-id <id>        Apply the view's filter, sort, and group\n  --take <n>            Records to return (default 100, max 1000)\n  --skip <n>            Offset for pagination\n  --projection <value>  Field IDs/names as JSON array, or all, e.g. '[\"Name\",\"Status\"]'\n  --search <json>       Fuzzy search: '{\"value\":\"keyword\"}' or '{\"value\":\"keyword\",\"fieldId\":\"Name\"}'\n  --record-id <id>      Fetch a single record by ID (ignores search/take/skip)\n\nNotes:\n  Default: data queries and bulk operations.\n  With --view-id: returns rows exactly as the UI shows them, group headers excluded.\n  Use --view-id for view-relative operations (\"the 3rd row\", \"row 5\").\n  Response is tabular: { header: [\"id\",\"Name\"], rows: [[\"rec001\",\"Task A\"]] }\n\nExamples:\n  teable record get --table-id tblXXX\n  teable record get --table-id tblXXX --view-id viwXXX --take 50\n  teable record get --table-id tblXXX --search '{\"value\":\"keyword\"}'\n  teable record get --table-id tblXXX --record-id recXXX",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--limit <number>",
              "description": "Alias for --take"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--projection <projection>",
              "description": "Projection of field ids to return, pass \"all\" to return all fields (max 50)",
              "default": [
                "all"
              ]
            },
            {
              "flags": "--record-id <id>",
              "description": "Record ID to fetch a specific record directly. When provided, search/take/skip are ignored."
            },
            {
              "flags": "--search <json>",
              "description": "Search filter. Pass { value: \"keyword\" } for global search, or { value: \"keyword\", fieldId: \"Name\" } to search a specific field"
            },
            {
              "flags": "--skip <number>",
              "description": "Number of records to skip for pagination"
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to query records from",
              "required": true
            },
            {
              "flags": "--take <number>",
              "description": "Number of records to return (default: 100, max: 1000)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--view-id <viewId>",
              "description": "View ID to use for filtering (optional)"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable record get [options]\n\nRetrieve records from a table in compact tabular format.\n\nOptions:\n  --table-id <id>       (required) Table to query\n  --view-id <id>        Apply the view's filter, sort, and group\n  --take <n>            Records to return (default 100, max 1000)\n  --skip <n>            Offset for pagination\n  --projection <value>  Field IDs/names as JSON array, or all, e.g. '[\"Name\",\"Status\"]'\n  --search <json>       Fuzzy search: '{\"value\":\"keyword\"}' or\n'{\"value\":\"keyword\",\"fieldId\":\"Name\"}'\n  --record-id <id>      Fetch a single record by ID (ignores search/take/skip)\n\nNotes:\n  Default: data queries and bulk operations.\n  With --view-id: returns rows exactly as the UI shows them, group headers excluded.\n  Use --view-id for view-relative operations (\"the 3rd row\", \"row 5\").\n  Response is tabular: { header: [\"id\",\"Name\"], rows: [[\"rec001\",\"Task A\"]] }\n\nExamples:\n  teable record get --table-id tblXXX\n  teable record get --table-id tblXXX --view-id viwXXX --take 50\n  teable record get --table-id tblXXX --search '{\"value\":\"keyword\"}'\n  teable record get --table-id tblXXX --record-id recXXX\n\nOptions:\n  --table-id <tableId>       The ID of the table to query records from\n  --view-id <viewId>         View ID to use for filtering (optional)\n  --take <number>            Number of records to return (default: 100, max: 1000)\n  --limit <number>           Alias for --take\n  --skip <number>            Number of records to skip for pagination\n  --projection <projection>  Projection of field ids to return, pass \"all\" to return all fields (max\n                             50) (default: [\"all\"])\n  --search <json>            Search filter. Pass { value: \"keyword\" } for global search, or { value:\n                             \"keyword\", fieldId: \"Name\" } to search a specific field\n  --record-id <id>           Record ID to fetch a specific record directly. When provided,\n                             search/take/skip are ignored.\n  -b, --base-id <baseId>     Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                             base-id <id>`.\n  --token <token>            Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>      Teable API server base URL\n  --pretty                   Indent JSON for human readability (default: false)\n  -h, --help                 display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update existing records in a table",
          "description": "Update records using compact array format.\n\nFORMAT:\n{\n  \"tableId\": \"tblXXX\",\n  \"header\": [\"recordId\", \"Name\", \"Status\"],\n  \"records\": [\n    [\"recAAA\", \"Updated Name A\", \"Done\"],\n    [\"recBBB\", \"Updated Name B\", \"Pending\"]\n  ]\n}\n\nRULES:\n- header: First element MUST be \"recordId\"\n- records: First value in each row is the record ID to update\n- Empty string \"\": Skip field (no change)\n- null: Clear the cell\n\nVALUE TYPES: Same as create-records\n\nREORDER (optional):\n  \"order\": { \"viewId\": \"viwXXX\", \"anchorId\": \"recYYY\", \"position\": \"before\"|\"after\" }\n  Moves record(s) before/after the anchor in the given view.\n  Swap rows A↔B: call twice with different anchors.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--header <jsonArray>",
              "description": "Array of field names. First element MUST be \"recordId\". Example: [\"recordId\", \"Name\", \"Status\"]"
            },
            {
              "flags": "--order <jsonObject>",
              "description": "Reorder record(s) relative to anchor. For swapping, call twice."
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--records <jsonArray>",
              "description": "Array of record rows. First value in each row is the record ID. Example: [[\"recAAA\", \"New Name\", \"Done\"]]",
              "required": true
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table containing the records",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--typecast",
              "description": "Automatically convert field values to proper types"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable record update [options]\n\nUpdate records using compact array format.\n\nFORMAT:\n{\n  \"tableId\": \"tblXXX\",\n  \"header\": [\"recordId\", \"Name\", \"Status\"],\n  \"records\": [\n    [\"recAAA\", \"Updated Name A\", \"Done\"],\n    [\"recBBB\", \"Updated Name B\", \"Pending\"]\n  ]\n}\n\nRULES:\n- header: First element MUST be \"recordId\"\n- records: First value in each row is the record ID to update\n- Empty string \"\": Skip field (no change)\n- null: Clear the cell\n\nVALUE TYPES: Same as create-records\n\nREORDER (optional):\n  \"order\": { \"viewId\": \"viwXXX\", \"anchorId\": \"recYYY\", \"position\": \"before\"|\"after\" }\n  Moves record(s) before/after the anchor in the given view.\n  Swap rows A↔B: call twice with different anchors.\n\nOptions:\n  --table-id <tableId>    The ID of the table containing the records\n  --header <jsonArray>    Array of field names. First element MUST be \"recordId\". Example:\n                          [\"recordId\", \"Name\", \"Status\"]\n  --records <jsonArray>   Array of record rows. First value in each row is the record ID. Example:\n                          [[\"recAAA\", \"New Name\", \"Done\"]]\n  --typecast              Automatically convert field values to proper types\n  --order <jsonObject>    Reorder record(s) relative to anchor. For swapping, call twice.\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        }
      ],
      "help": "Usage: teable record [options] [command]\n\nrecord operations (create, get, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create new records in a table\n  get [options]     Query records (--view-id applies filter/sort/group, --search for fuzzy match,\n                    --record-id for single record)\n  update [options]  Update existing records in a table\n  delete [options]  Delete records from a table\n  help [command]    display help for command\n\nExamples:\n  teable record get --table-id tblXXXX\n  teable record get --table-id tblXXXX --view-id viwXXXX\n  teable record create --table-id tblXXXX --records '[{\"fields\":{\"Name\":\"Alice\"}}]'\n  teable record delete --table-id tblXXXX --record-ids rec001,rec002\n"
    },
    {
      "name": "sandbox",
      "description": "sandbox operations (tool)",
      "subcommands": [
        {
          "name": "tool",
          "summary": "Manage tools that survive sandbox rebuilds (upsert, setup, list, delete)",
          "description": "Manage the tools recorded on the server: state directories are symlinked back whenever the sandbox is rebuilt, and `setup <name>` replays the recorded setup command on demand.",
          "subcommands": [
            {
              "name": "delete",
              "summary": "Delete a recorded tool by name",
              "description": "Forget this tool: drop its setup record and delete the state it had persisted, clearing the symlinks that pointed at it. To change a tool, run `upsert` again instead: deleting and re-adding would destroy the state in between.",
              "arguments": [
                {
                  "name": "name",
                  "required": true,
                  "description": "Tool name (from `teable sandbox tool list`)"
                }
              ],
              "options": [
                {
                  "flags": "--endpoint <endpoint>",
                  "description": "Teable API server base URL"
                },
                {
                  "flags": "--pretty",
                  "description": "Indent JSON for human readability",
                  "default": false
                },
                {
                  "flags": "--token <token>",
                  "description": "Personal access token; falls back to TEABLE_TOKEN env var"
                }
              ],
              "help": "Usage: teable sandbox tool delete [options] <name>\n\nForget this tool: drop its setup record and delete the state it had persisted, clearing the symlinks\nthat pointed at it. To change a tool, run `upsert` again instead: deleting and re-adding would\ndestroy the state in between.\n\nArguments:\n  name                   Tool name (from `teable sandbox tool list`)\n\nOptions:\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n",
              "availability": "sandbox-only"
            },
            {
              "name": "list",
              "summary": "List tools recorded for sandbox rebuilds",
              "description": "List the tools recorded for this sandbox, with their setup command and persisted $HOME-relative paths.",
              "options": [
                {
                  "flags": "--endpoint <endpoint>",
                  "description": "Teable API server base URL"
                },
                {
                  "flags": "--pretty",
                  "description": "Indent JSON for human readability",
                  "default": false
                },
                {
                  "flags": "--token <token>",
                  "description": "Personal access token; falls back to TEABLE_TOKEN env var"
                }
              ],
              "help": "Usage: teable sandbox tool list [options]\n\nList the tools recorded for this sandbox, with their setup command and persisted $HOME-relative\npaths.\n\nOptions:\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n",
              "availability": "sandbox-only"
            },
            {
              "name": "setup",
              "summary": "Run a recorded tool's setup command in this sandbox",
              "description": "Run a recorded tool's setup command in this sandbox, exactly as recorded. Recorded tools are not set up automatically after a rebuild — run this before first use; a missing binary right after a rebuild usually just means this has not run yet. State directories are already relinked at startup, so setup and state compose.",
              "arguments": [
                {
                  "name": "name",
                  "required": true,
                  "description": "Tool name (from `teable sandbox tool list`)"
                }
              ],
              "options": [
                {
                  "flags": "--endpoint <endpoint>",
                  "description": "Teable API server base URL"
                },
                {
                  "flags": "--pretty",
                  "description": "Indent JSON for human readability",
                  "default": false
                },
                {
                  "flags": "--token <token>",
                  "description": "Personal access token; falls back to TEABLE_TOKEN env var"
                }
              ],
              "help": "Usage: teable sandbox tool setup [options] <name>\n\nRun a recorded tool's setup command in this sandbox, exactly as recorded. Recorded tools are not set\nup automatically after a rebuild — run this before first use; a missing binary right after a rebuild\nusually just means this has not run yet. State directories are already relinked at startup, so setup\nand state compose.\n\nArguments:\n  name                   Tool name (from `teable sandbox tool list`)\n\nOptions:\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n",
              "availability": "sandbox-only"
            },
            {
              "name": "upsert",
              "summary": "Record or update a tool so it survives sandbox rebuilds",
              "description": "Record one tool: its setup command, the $HOME directories holding state that command cannot regenerate, a description — each optional, in any combination. Keyed by name — running this again updates the record in place (paths accumulate); never delete and re-add, which would destroy the state in between. A path belongs to exactly one tool: record a companion sharing the directory with --command only. Existing ~/.<name>, ~/.local/share/<name> and ~/.local/state/<name> dirs are discovered and persisted automatically. Each --path is moved into ~/.teable/home now and symlinked back; after a rebuild the symlinks are restored and `setup <name>` replays the command on demand. Always give a --description: the agent decides from it whether a recorded tool fits a task. Pin versions in the command (e.g. npm i -g <package>@<version>) for reproducible rebuilds. Prefer $KEY references (stored via `teable env set`) over embedding secrets in the command.",
              "options": [
                {
                  "flags": "--endpoint <endpoint>",
                  "description": "Teable API server base URL"
                },
                {
                  "flags": "--path <path>",
                  "description": "State directory to persist, e.g. ~/.<tool> or ~/.local/share/<tool>; repeatable",
                  "default": []
                },
                {
                  "flags": "--pretty",
                  "description": "Indent JSON for human readability",
                  "default": false
                },
                {
                  "flags": "--token <token>",
                  "description": "Personal access token; falls back to TEABLE_TOKEN env var"
                },
                {
                  "flags": "-c, --command <command>",
                  "description": "Shell command `setup <name>` replays after a rebuild"
                },
                {
                  "flags": "-d, --description <text>",
                  "description": "One line of what the tool is for"
                },
                {
                  "flags": "-n, --name <name>",
                  "description": "Tool-like identifier (upsert key)",
                  "required": true
                }
              ],
              "help": "Usage: teable sandbox tool upsert [options]\n\nRecord one tool: its setup command, the $HOME directories holding state that command cannot\nregenerate, a description — each optional, in any combination. Keyed by name — running this again\nupdates the record in place (paths accumulate); never delete and re-add, which would destroy the\nstate in between. A path belongs to exactly one tool: record a companion sharing the directory with\n--command only. Existing ~/.<name>, ~/.local/share/<name> and ~/.local/state/<name> dirs are\ndiscovered and persisted automatically. Each --path is moved into ~/.teable/home now and symlinked\nback; after a rebuild the symlinks are restored and `setup <name>` replays the command on demand.\nAlways give a --description: the agent decides from it whether a recorded tool fits a task. Pin\nversions in the command (e.g. npm i -g <package>@<version>) for reproducible rebuilds. Prefer $KEY\nreferences (stored via `teable env set`) over embedding secrets in the command.\n\nOptions:\n  -n, --name <name>         Tool-like identifier (upsert key)\n  -c, --command <command>   Shell command `setup <name>` replays after a rebuild\n  -d, --description <text>  One line of what the tool is for\n  --path <path>             State directory to persist, e.g. ~/.<tool> or ~/.local/share/<tool>;\n                            repeatable (default: [])\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n",
              "availability": "sandbox-only"
            }
          ],
          "help": "Usage: teable sandbox tool [options] [command]\n\nManage the tools recorded on the server: state directories are symlinked back whenever the sandbox\nis rebuilt, and `setup <name>` replays the recorded setup command on demand.\n\nOptions:\n  -h, --help               display help for command\n\nCommands:\n  upsert [options]         Record or update a tool so it survives sandbox rebuilds\n  setup [options] <name>   Run a recorded tool's setup command in this sandbox\n  list [options]           List tools recorded for sandbox rebuilds\n  delete [options] <name>  Delete a recorded tool by name\n  help [command]           display help for command\n",
          "availability": "sandbox-only"
        }
      ],
      "help": "Usage: teable sandbox [options] [command]\n\nsandbox operations (tool)\n\nOptions:\n  -h, --help      display help for command\n\nCommands:\n  tool            Manage tools that survive sandbox rebuilds (upsert, setup, list, delete)\n  help [command]  display help for command\n\nExamples:\n  teable sandbox tool upsert --name <tool> --command \"<install command>\"\n  teable sandbox tool upsert --name <tool> --path ~/.<dirname>   # state under a different name\n  teable sandbox tool list\n  teable sandbox tool delete <name>\n",
      "availability": "sandbox-only"
    },
    {
      "name": "scrape",
      "summary": "Scrape structured data from URLs — supports LinkedIn, Amazon, Instagram, TikTok, YouTube, X/Twitter, Reddit, and 35+ more platforms",
      "description": "Scrape structured data from 44+ web platforms. Two modes:\n  (1) TRIGGER: pass datasetId + inputs to start a new scrape\n  (2) POLL: pass only snapshotId to check status of a pending scrape\n\nMatch URL to datasetId:\nlinkedin.com: /in/→linkedin_person_profile, /company/→linkedin_company_profile, /jobs/→linkedin_job_listings, /pulse/|/posts/→linkedin_posts\namazon.*: /dp/→amazon_product, /dp/(reviews)→amazon_product_reviews, (search)→amazon_product_search(needs keyword+url)\ninstagram.com: /<user>→instagram_profiles, /p/→instagram_posts, /reel/→instagram_reels\nx.com|twitter.com: /status/→x_posts, /<user>→x_profile_posts\nyoutube.com: /watch→youtube_videos, /@channel→youtube_profiles, (comments)→youtube_comments\ntiktok.com: /@user→tiktok_profiles, /@user/video/→tiktok_posts\nOthers: reddit_posts, google_maps_reviews(needs days_limit), facebook_posts|facebook_marketplace_listings|facebook_company_reviews, github_repository_file, zillow_properties_listing, walmart_product, booking_hotel_listings\n\nMost datasets need only [{ \"url\": \"...\" }]. For full 44+ list: get-doc topic \"scrape.datasets\".\nBatch same-datasetId URLs in one call: inputs: [{ \"url\": \"url1\" }, { \"url\": \"url2\" }].\n\nPolling: If result has { pending: true, snapshotId }, call wait(seconds: 10) then call scrape again WITH that snapshotId (POLL mode). Never omit snapshotId (that triggers a new scrape). Repeat until data or error.",
      "options": [
        {
          "flags": "--dataset-id <datasetId>",
          "description": "TRIGGER mode only (required). Dataset ID from the URL-pattern list above. Use get-doc topic \"scrape.datasets\" if URL doesn't match any pattern. Do NOT pass when polling."
        },
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--inputs <jsonArray>",
          "description": "TRIGGER mode only (required). Array of input objects. Most datasets need only [{ \"url\": \"https://...\" }]. Pass multiple objects to batch scrape: [{ \"url\": \"...\" }, { \"url\": \"...\" }]. Exceptions: amazon_product_search needs \"keyword\"+\"url\", linkedin_people_search needs \"url\"+\"first_name\"+\"last_name\". Do NOT pass when polling."
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--snapshot-id <snapshotId>",
          "description": "POLL mode only. Pass the snapshotId from a previous pending result to check its status. When provided, do NOT pass datasetId or inputs."
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable scrape [options]\n\nScrape structured data from 44+ web platforms. Two modes:\n  (1) TRIGGER: pass datasetId + inputs to start a new scrape\n  (2) POLL: pass only snapshotId to check status of a pending scrape\n\nMatch URL to datasetId:\nlinkedin.com: /in/→linkedin_person_profile, /company/→linkedin_company_profile,\n/jobs/→linkedin_job_listings, /pulse/|/posts/→linkedin_posts\namazon.*: /dp/→amazon_product, /dp/(reviews)→amazon_product_reviews,\n(search)→amazon_product_search(needs keyword+url)\ninstagram.com: /<user>→instagram_profiles, /p/→instagram_posts, /reel/→instagram_reels\nx.com|twitter.com: /status/→x_posts, /<user>→x_profile_posts\nyoutube.com: /watch→youtube_videos, /@channel→youtube_profiles, (comments)→youtube_comments\ntiktok.com: /@user→tiktok_profiles, /@user/video/→tiktok_posts\nOthers: reddit_posts, google_maps_reviews(needs days_limit),\nfacebook_posts|facebook_marketplace_listings|facebook_company_reviews, github_repository_file,\nzillow_properties_listing, walmart_product, booking_hotel_listings\n\nMost datasets need only [{ \"url\": \"...\" }]. For full 44+ list: get-doc topic \"scrape.datasets\".\nBatch same-datasetId URLs in one call: inputs: [{ \"url\": \"url1\" }, { \"url\": \"url2\" }].\n\nPolling: If result has { pending: true, snapshotId }, call wait(seconds: 10) then call scrape again\nWITH that snapshotId (POLL mode). Never omit snapshotId (that triggers a new scrape). Repeat until\ndata or error.\n\nOptions:\n  --dataset-id <datasetId>    TRIGGER mode only (required). Dataset ID from the URL-pattern list\n                              above. Use get-doc topic \"scrape.datasets\" if URL doesn't match any\n                              pattern. Do NOT pass when polling.\n  --inputs <jsonArray>        TRIGGER mode only (required). Array of input objects. Most datasets\n                              need only [{ \"url\": \"https://...\" }]. Pass multiple objects to batch\n                              scrape: [{ \"url\": \"...\" }, { \"url\": \"...\" }]. Exceptions:\n                              amazon_product_search needs \"keyword\"+\"url\", linkedin_people_search\n                              needs \"url\"+\"first_name\"+\"last_name\". Do NOT pass when polling.\n  --snapshot-id <snapshotId>  POLL mode only. Pass the snapshotId from a previous pending result to\n                              check its status. When provided, do NOT pass datasetId or inputs.\n  -b, --base-id <baseId>      Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                              base-id <id>`.\n  --token <token>             Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>       Teable API server base URL\n  --pretty                    Indent JSON for human readability (default: false)\n  -h, --help                  display help for command\n"
    },
    {
      "name": "search-api",
      "summary": "Search available APIs by description to find the right endpoint",
      "description": "Search available Teable APIs by keyword or description.\n\nReturns APIs matching your permission level. Use with call-api to execute.\n\nParameters:\n  - query (required): Search keywords in any language\n  - tags: Filter by API category (e.g., [\"record\", \"field\"])\n  - limit: Max results (default: 5, max: 10)\n\nReturns API details including:\n  - method, url: Pass directly to call-api\n  - summary, description\n  - parameters: Required and optional params with descriptions\n  - requestBody: Body schema for POST/PUT/PATCH endpoints\n\nExample: search-api({ query: \"update records\" })\n  → { method: \"PATCH\", url: \"/table/{tableId}/record\", parameters: [...], requestBody: {...} }\n  → call-api({ method: \"PATCH\", url: \"/table/{tableId}/record\", params: { tableId: \"tblXXX\" }, data: { ... } })",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--keyword <query>",
          "description": "Alias for --query"
        },
        {
          "flags": "--limit <number>",
          "description": "Maximum number of results to return (default: 5, max: 10)"
        },
        {
          "flags": "--max-results <number>",
          "description": "Alias for --limit"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--query <query>",
          "description": "Search keywords (e.g., \"duplicate record\", \"get fields\", \"create automation\")",
          "required": true
        },
        {
          "flags": "--search <query>",
          "description": "Alias for --query"
        },
        {
          "flags": "--tags <jsonArray>",
          "description": "Optional tag filter to narrow search (e.g., [\"record\", \"field\"])"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable search-api [options]\n\nSearch available Teable APIs by keyword or description.\n\nReturns APIs matching your permission level. Use with call-api to execute.\n\nParameters:\n  - query (required): Search keywords in any language\n  - tags: Filter by API category (e.g., [\"record\", \"field\"])\n  - limit: Max results (default: 5, max: 10)\n\nReturns API details including:\n  - method, url: Pass directly to call-api\n  - summary, description\n  - parameters: Required and optional params with descriptions\n  - requestBody: Body schema for POST/PUT/PATCH endpoints\n\nExample: search-api({ query: \"update records\" })\n  → { method: \"PATCH\", url: \"/table/{tableId}/record\", parameters: [...], requestBody: {...} }\n  → call-api({ method: \"PATCH\", url: \"/table/{tableId}/record\", params: { tableId: \"tblXXX\" }, data:\n{ ... } })\n\nOptions:\n  --query <query>         Search keywords (e.g., \"duplicate record\", \"get fields\", \"create\n                          automation\")\n  --search <query>        Alias for --query\n  --keyword <query>       Alias for --query\n  --tags <jsonArray>      Optional tag filter to narrow search (e.g., [\"record\", \"field\"])\n  --limit <number>        Maximum number of results to return (default: 5, max: 10)\n  --max-results <number>  Alias for --limit\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "send-email",
      "summary": "Send an email to one or more recipients (system SMTP by default, or --smtp for a custom transport)",
      "description": "Send an email via the Teable mail sender API.",
      "options": [
        {
          "flags": "--bcc <bcc>",
          "description": "Comma-separated BCC emails. Required unless --to is given"
        },
        {
          "flags": "--body <body>",
          "description": "Email body (markdown/HTML string, or path to a file)",
          "required": true
        },
        {
          "flags": "--body-type <bodyType>",
          "description": "Body type: markdown or html",
          "default": "markdown"
        },
        {
          "flags": "--cc <cc>",
          "description": "Comma-separated CC emails"
        },
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--reply-to <replyTo>",
          "description": "Reply-to email address"
        },
        {
          "flags": "--smtp <smtp>",
          "description": "SMTP config as JSON string"
        },
        {
          "flags": "--subject <subject>",
          "description": "Email subject",
          "required": true
        },
        {
          "flags": "--to <to>",
          "description": "Comma-separated recipient emails. Required unless --bcc is given"
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable send-email [options]\n\nSend an email via the Teable mail sender API.\n\nOptions:\n  --subject <subject>     Email subject\n  --body <body>           Email body (markdown/HTML string, or path to a file)\n  --to <to>               Comma-separated recipient emails. Required unless --bcc is given\n  --cc <cc>               Comma-separated CC emails\n  --bcc <bcc>             Comma-separated BCC emails. Required unless --to is given\n  --body-type <bodyType>  Body type: markdown or html (default: \"markdown\")\n  --reply-to <replyTo>    Reply-to email address\n  --smtp <smtp>           SMTP config as JSON string\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "skill",
      "description": "skill operations (list, import-github, import-file, update)",
      "subcommands": [
        {
          "name": "import-file",
          "summary": "Import a skill from a local .skill or .zip file",
          "description": "Import a skill from a .skill or .zip file on disk. Both extensions are accepted; the file content must be a valid ZIP archive (a .skill file is a renamed .zip).",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--scope <scopeType>",
              "description": "Alias for --scope-type"
            },
            {
              "flags": "--scope-id <scopeId>",
              "description": "Scope entity ID (required for base/space/app/cuppyclaw; app defaults to TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)"
            },
            {
              "flags": "--scope-type <scopeType>",
              "description": "One of: user, base, space, system, app, cuppyclaw",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-f, --file-path <path>",
              "description": "Path to the .skill or .zip file (content must be a valid ZIP archive)",
              "required": true
            }
          ],
          "help": "Usage: teable skill import-file [options]\n\nImport a skill from a .skill or .zip file on disk. Both extensions are accepted; the file content\nmust be a valid ZIP archive (a .skill file is a renamed .zip).\n\nOptions:\n  --scope-type <scopeType>  One of: user, base, space, system, app, cuppyclaw\n  --scope <scopeType>       Alias for --scope-type\n  -f, --file-path <path>    Path to the .skill or .zip file (content must be a valid ZIP archive)\n  --scope-id <scopeId>      Scope entity ID (required for base/space/app/cuppyclaw; app defaults to\n                            TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "import-github",
          "summary": "Import a skill from a GitHub repository URL",
          "description": "Import a skill from a GitHub URL pointing to a skill folder via /tree/<ref>/<path> or /blob/<ref>/<path>. Plain repo URLs are rejected.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--scope <scopeType>",
              "description": "Alias for --scope-type"
            },
            {
              "flags": "--scope-id <scopeId>",
              "description": "Scope entity ID (required for base/space/app/cuppyclaw; app defaults to TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)"
            },
            {
              "flags": "--scope-type <scopeType>",
              "description": "One of: user, base, space, system, app, cuppyclaw",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-u, --url <url>",
              "description": "GitHub URL to a skill folder (must include /tree/<ref>/<path> or /blob/<ref>/<path>), e.g. https://github.com/owner/repo/tree/main/skills/my-skill",
              "required": true
            }
          ],
          "help": "Usage: teable skill import-github [options]\n\nImport a skill from a GitHub URL pointing to a skill folder via /tree/<ref>/<path> or\n/blob/<ref>/<path>. Plain repo URLs are rejected.\n\nOptions:\n  --scope-type <scopeType>  One of: user, base, space, system, app, cuppyclaw\n  --scope <scopeType>       Alias for --scope-type\n  -u, --url <url>           GitHub URL to a skill folder (must include /tree/<ref>/<path> or\n                            /blob/<ref>/<path>), e.g.\n                            https://github.com/owner/repo/tree/main/skills/my-skill\n  --scope-id <scopeId>      Scope entity ID (required for base/space/app/cuppyclaw; app defaults to\n                            TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "list",
          "summary": "List installed or available skills by scope",
          "description": "List managed skills (full detail) or available skills (for chat slash-commands).",
          "options": [
            {
              "flags": "--base-id <baseId>",
              "description": "Base ID (includes base-scoped skills for user scope)"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--list-type <type>",
              "description": "managed or available",
              "choices": [
                "managed",
                "available"
              ],
              "default": "managed"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--scope <scopeType>",
              "description": "Alias for --scope-type"
            },
            {
              "flags": "--scope-id <scopeId>",
              "description": "Scope entity ID (required for base/space/app/cuppyclaw; app defaults to TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)"
            },
            {
              "flags": "--scope-type <scopeType>",
              "description": "One of: user, base, space, system, app, cuppyclaw",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            }
          ],
          "help": "Usage: teable skill list [options]\n\nList managed skills (full detail) or available skills (for chat slash-commands).\n\nOptions:\n  --scope-type <scopeType>  One of: user, base, space, system, app, cuppyclaw\n  --scope <scopeType>       Alias for --scope-type\n  --list-type <type>        managed or available (choices: \"managed\", \"available\", default:\n                            \"managed\")\n  --scope-id <scopeId>      Scope entity ID (required for base/space/app/cuppyclaw; app defaults to\n                            TEABLE_APP_ID, cuppyclaw to TEABLE_BOT_ID)\n  --base-id <baseId>        Base ID (includes base-scoped skills for user scope)\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update skill icon or enabled state",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--is-enabled <bool>",
              "description": "true or false",
              "required": true,
              "choices": [
                "true",
                "false"
              ]
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--skill-id <id>",
              "description": "Skill ID (sklXXXX)",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            }
          ],
          "help": "Usage: teable skill update [options]\n\nOptions:\n  --skill-id <id>        Skill ID (sklXXXX)\n  --is-enabled <bool>    true or false (choices: \"true\", \"false\")\n  --token <token>        Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>  Teable API server base URL\n  --pretty               Indent JSON for human readability (default: false)\n  -h, --help             display help for command\n"
        }
      ],
      "help": "Usage: teable skill [options] [command]\n\nskill operations (list, import-github, import-file, update)\n\nOptions:\n  -h, --help               display help for command\n\nCommands:\n  list [options]           List installed or available skills by scope\n  import-github [options]  Import a skill from a GitHub repository URL\n  import-file [options]    Import a skill from a local .skill or .zip file\n  update [options]         Update skill icon or enabled state\n  help [command]           display help for command\n\nExamples:\n  teable skill list --scope user\n  teable skill import-github --scope user --url https://github.com/owner/repo/tree/main/skills/my-skill\n  teable skill import-file --scope user --file-path ./my-skill.skill\n  teable skill update --skill-id sklXXXX --is-enabled true\n"
    },
    {
      "name": "sql-query",
      "summary": "Execute SQL query on tables",
      "description": "\n⚠️ READ-ONLY SQL Query Tool - Only SELECT statements allowed. No INSERT/UPDATE/DELETE/DDL.\n\nExecute PostgreSQL 15.4 queries and return results.\n\n## CRITICAL: Use Database Names, NOT Display Names or IDs\n\nBefore executing SQL, you MUST:\n1. get dbTableName (NOT the table's display name)\n2. get dbFieldName for each field (NOT the field ID like fldXXX)\n\n## SQL Generation Rules\n\n### Table Reference\n- Table format: \"baseId\".\"dbTableName\" (e.g., \"bseXXX\".\"receipts\")\n- ❌ WRONG: \"bseXXX\".\"Receipts\" (display name)\n- ✅ CORRECT: \"bseXXX\".\"receipts\" (dbTableName)\n\n### Column Reference  \n- Use dbFieldName from get-fields (e.g., \"vendor_name\", \"total_amount\")\n- ❌ WRONG: \"fld1Y0f3b2ryDV9N2MR\" (field ID)\n- ✅ CORRECT: \"vendor_name\" (dbFieldName)\n\n### System Fields (always available)\n- \"__id\" - Record ID (text)\n- \"__auto_number\" - Auto-increment number (int)\n- \"__created_time\" - Creation timestamp (timestamptz, UTC)\n- \"__last_modified_time\" - Last modified timestamp (timestamptz, UTC, nullable)\n- \"__created_by\" - Creator user ID (varchar)\n- \"__last_modified_by\" - Last modifier user ID (varchar, nullable)\n- \"__version\" - Record version (int)\n\n### Identifiers & Syntax\n- Use double quotes for ALL identifiers: SELECT \"field\" FROM \"schema\".\"table\"\n\n### Result Limits\n- Non-aggregate queries: Always add LIMIT 100\n- Aggregate/statistical queries: No limit required\n\n### Data Type Handling\n- **Numbers**: Use CAST(COUNT(*) AS text) for aggregates to avoid overflow\n- **Dates**: Database stores UTC. Convert timezone: timezone('Asia/Shanghai', date_column)\n  Format with: to_char(timezone('TZ', col), 'YYYY-MM-DD HH24:MI:SS')\n- **JSON fields**: Use json_extract_path_text(\"field\"::json, 'key') to extract values\n\n## Parameters\n- sql: The SELECT query using dbTableName and dbFieldName\n",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--query <sql>",
          "description": "Alias for --sql"
        },
        {
          "flags": "--sql <sql>",
          "description": "SQL query to execute (SELECT only)",
          "required": true
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable sql-query [options]\n\n\n⚠️ READ-ONLY SQL Query Tool - Only SELECT statements allowed. No INSERT/UPDATE/DELETE/DDL.\n\nExecute PostgreSQL 15.4 queries and return results.\n\n## CRITICAL: Use Database Names, NOT Display Names or IDs\n\nBefore executing SQL, you MUST:\n1. get dbTableName (NOT the table's display name)\n2. get dbFieldName for each field (NOT the field ID like fldXXX)\n\n## SQL Generation Rules\n\n### Table Reference\n- Table format: \"baseId\".\"dbTableName\" (e.g., \"bseXXX\".\"receipts\")\n- ❌ WRONG: \"bseXXX\".\"Receipts\" (display name)\n- ✅ CORRECT: \"bseXXX\".\"receipts\" (dbTableName)\n\n### Column Reference\n- Use dbFieldName from get-fields (e.g., \"vendor_name\", \"total_amount\")\n- ❌ WRONG: \"fld1Y0f3b2ryDV9N2MR\" (field ID)\n- ✅ CORRECT: \"vendor_name\" (dbFieldName)\n\n### System Fields (always available)\n- \"__id\" - Record ID (text)\n- \"__auto_number\" - Auto-increment number (int)\n- \"__created_time\" - Creation timestamp (timestamptz, UTC)\n- \"__last_modified_time\" - Last modified timestamp (timestamptz, UTC, nullable)\n- \"__created_by\" - Creator user ID (varchar)\n- \"__last_modified_by\" - Last modifier user ID (varchar, nullable)\n- \"__version\" - Record version (int)\n\n### Identifiers & Syntax\n- Use double quotes for ALL identifiers: SELECT \"field\" FROM \"schema\".\"table\"\n\n### Result Limits\n- Non-aggregate queries: Always add LIMIT 100\n- Aggregate/statistical queries: No limit required\n\n### Data Type Handling\n- **Numbers**: Use CAST(COUNT(*) AS text) for aggregates to avoid overflow\n- **Dates**: Database stores UTC. Convert timezone: timezone('Asia/Shanghai', date_column)\n  Format with: to_char(timezone('TZ', col), 'YYYY-MM-DD HH24:MI:SS')\n- **JSON fields**: Use json_extract_path_text(\"field\"::json, 'key') to extract values\n\n## Parameters\n- sql: The SELECT query using dbTableName and dbFieldName\n\n\nOptions:\n  --sql <sql>             SQL query to execute (SELECT only)\n  --query <sql>           Alias for --sql\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n\nExamples:\n  teable sql-query --sql 'SELECT * FROM \"bseXXX\".\"tbl_users\" LIMIT 10'\n  # dbTableName (e.g. bseXXX.tbl_users) splits into \"bseXXX\".\"tbl_users\"\n"
    },
    {
      "name": "table",
      "description": "table operations (create, get, update, delete)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create a new table with fields",
          "description": "Create a table with fields using array format.\n\nFORMAT:\n{\n  \"tableName\": \"Tasks\",\n  \"fields\": [\n    \"Title:text\",\n    \"Status:sel:Todo,In Progress,Done\",\n    \"Priority:sel:P0,P1,P2\",\n    \"Assignee:user\",\n    \"Due:date\"\n  ]\n}\n\nFIELD STRING SYNTAX: \"Name:Type\" or \"Name:Type:Options\"\n\nTYPES:\n  text       - Single line text\n  long       - Long text / notes\n  num        - Number\n  date       - Date/time\n  check      - Checkbox\n  rate       - Rating (1-5 stars)\n  user       - User assignment\n  file       - Attachments\n  auto       - Auto-incrementing ID\n  created    - Created time\n  modified   - Last modified time\n  sel:A,B,C  - Single select with choices\n  mul:A,B,C  - Multiple select with choices\n\nEXAMPLES:\n  Simple: { \"tableName\": \"Notes\", \"fields\": [\"Title:text\", \"Content:long\"] }\n  Task tracker: { \"tableName\": \"Tasks\", \"fields\": [\"Task:text\", \"Status:sel:Todo,Done\", \"Due:date\"] }",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--fields <jsonArray>",
              "description": "Array of field definitions using shorthand syntax",
              "required": true
            },
            {
              "flags": "--first-view-name <firstViewName>",
              "description": "Name for the first view"
            },
            {
              "flags": "--folder-id <folderId>",
              "description": "Folder to place the new table in (`folderId` from Active Tab Meta); omit for base root."
            },
            {
              "flags": "--icon <emoji>",
              "description": "Emoji icon"
            },
            {
              "flags": "--name <tableName>",
              "description": "Alias for --table-name"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-name <tableName>",
              "description": "Table name (no emoji in name)",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable table create [options]\n\nCreate a table with fields using array format.\n\nFORMAT:\n{\n  \"tableName\": \"Tasks\",\n  \"fields\": [\n    \"Title:text\",\n    \"Status:sel:Todo,In Progress,Done\",\n    \"Priority:sel:P0,P1,P2\",\n    \"Assignee:user\",\n    \"Due:date\"\n  ]\n}\n\nFIELD STRING SYNTAX: \"Name:Type\" or \"Name:Type:Options\"\n\nTYPES:\n  text       - Single line text\n  long       - Long text / notes\n  num        - Number\n  date       - Date/time\n  check      - Checkbox\n  rate       - Rating (1-5 stars)\n  user       - User assignment\n  file       - Attachments\n  auto       - Auto-incrementing ID\n  created    - Created time\n  modified   - Last modified time\n  sel:A,B,C  - Single select with choices\n  mul:A,B,C  - Multiple select with choices\n\nEXAMPLES:\n  Simple: { \"tableName\": \"Notes\", \"fields\": [\"Title:text\", \"Content:long\"] }\n  Task tracker: { \"tableName\": \"Tasks\", \"fields\": [\"Task:text\", \"Status:sel:Todo,Done\", \"Due:date\"]\n}\n\nOptions:\n  --table-name <tableName>           Table name (no emoji in name)\n  --name <tableName>                 Alias for --table-name\n  --fields <jsonArray>               Array of field definitions using shorthand syntax\n  --icon <emoji>                     Emoji icon\n  --first-view-name <firstViewName>  Name for the first view\n  --folder-id <folderId>             Folder to place the new table in (`folderId` from Active Tab\n                                     Meta); omit for base root.\n  -b, --base-id <baseId>             Target base ID (required, e.g. bseXXXX). Set default: `teable\n                                     config base-id <id>`.\n  --token <token>                    Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>              Teable API server base URL\n  --pretty                           Indent JSON for human readability (default: false)\n  -h, --help                         display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete a table (moves it to trash)",
          "description": "Delete a table (moves to trash, can be restored within retention period).\n\nParameters:\n  - tableId (required): The ID of the table to delete\n\n⚠️ Warning: This operation is destructive. Verify tableId before deletion.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to delete",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable table delete [options]\n\nDelete a table (moves to trash, can be restored within retention period).\n\nParameters:\n  - tableId (required): The ID of the table to delete\n\n⚠️ Warning: This operation is destructive. Verify tableId before deletion.\n\nOptions:\n  --table-id <tableId>    The ID of the table to delete\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "get",
          "summary": "Get tables metadata in a base",
          "description": "Get tables metadata in a base.\n\nParameters:\n  - baseId (required): The base ID\n  - tableId (optional): Filter by specific table ID\n  - tableName (optional): Filter by specific table name\n\nReturns table information:\n  - id: Unique table identifier\n  - name: Display name\n  - dbTableName: Database table name (use this for SQL queries)\n  - description: Table description\n  - icon: Table icon",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "Optional: filter by specific table ID"
            },
            {
              "flags": "--table-name <tableName>",
              "description": "Optional: filter by specific table name"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable table get [options]\n\nGet tables metadata in a base.\n\nParameters:\n  - baseId (required): The base ID\n  - tableId (optional): Filter by specific table ID\n  - tableName (optional): Filter by specific table name\n\nReturns table information:\n  - id: Unique table identifier\n  - name: Display name\n  - dbTableName: Database table name (use this for SQL queries)\n  - description: Table description\n  - icon: Table icon\n\nOptions:\n  --table-id <tableId>      Optional: filter by specific table ID\n  --table-name <tableName>  Optional: filter by specific table name\n  -b, --base-id <baseId>    Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                            base-id <id>`.\n  --token <token>           Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>     Teable API server base URL\n  --pretty                  Indent JSON for human readability (default: false)\n  -h, --help                display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update table name or description",
          "description": "Update table name or description.\n\nParameters:\n  - tableId (required): The table ID\n  - name (optional): New name for the table\n  - description (optional): New description\n\nAt least one of name or description must be provided.",
          "options": [
            {
              "flags": "--description <description>",
              "description": "New description for the table"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--name <name>",
              "description": "New name for the table"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to update",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable table update [options]\n\nUpdate table name or description.\n\nParameters:\n  - tableId (required): The table ID\n  - name (optional): New name for the table\n  - description (optional): New description\n\nAt least one of name or description must be provided.\n\nOptions:\n  --table-id <tableId>         The ID of the table to update\n  --name <name>                New name for the table\n  --description <description>  New description for the table\n  -b, --base-id <baseId>       Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                               base-id <id>`.\n  --token <token>              Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>        Teable API server base URL\n  --pretty                     Indent JSON for human readability (default: false)\n  -h, --help                   display help for command\n"
        }
      ],
      "help": "Usage: teable table [options] [command]\n\ntable operations (create, get, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create a new table with fields\n  get [options]     Get tables metadata in a base\n  update [options]  Update table name or description\n  delete [options]  Delete a table (moves it to trash)\n  help [command]    display help for command\n\nExamples:\n  teable table get --table-id tblXXXX\n  teable table create --name Tasks --fields '[\"Title:text\",\"Status:sel:Todo,Done\"]'\n  teable table update --table-id tblXXXX --name NewName\n  teable table delete --table-id tblXXXX\n"
    },
    {
      "name": "tools",
      "description": "Tool discovery and metadata commands",
      "subcommands": [
        {
          "name": "list",
          "description": "List tools available for current base and permission level",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--search <keyword>",
              "description": "Filter tools by name or brief (case-insensitive)"
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable tools list [options]\n\nList tools available for current base and permission level\n\nOptions:\n  --search <keyword>      Filter tools by name or brief (case-insensitive)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        }
      ],
      "help": "Usage: teable tools [options] [command]\n\nTool discovery and metadata commands\n\nOptions:\n  -h, --help      display help for command\n\nCommands:\n  list [options]  List tools available for current base and permission level\n  help [command]  display help for command\n"
    },
    {
      "name": "trigger-ai-fill",
      "summary": "Trigger AI generation for an AI-configured field",
      "description": "Trigger AI generation for an AI-configured field.\n\nIMPORTANT: Creating/updating a field with aiConfig does NOT automatically generate content.\nYou MUST call this tool after setting up an AI field to actually run the AI generation.\n\nMODES:\n  - Single cell: provide recordId to fill one specific record\n  - Batch fill: omit recordId to fill multiple records\n\nOPTIONS:\n  - mode: \"all\" (default) overwrites existing values\n  - mode: \"emptyOnly\" only fills cells that are currently empty\n  - viewId: limit to records visible in a specific view\n\nEXAMPLES:\n  1. Fill all records: { tableId, fieldId }\n  2. Fill one record: { tableId, fieldId, recordId: \"recXXX\" }\n  3. Fill empty only: { tableId, fieldId, mode: \"emptyOnly\" }\n\nReturns taskId for background processing. AI generation runs asynchronously.\nSuccess means the background task was accepted, not that generated values are ready.",
      "options": [
        {
          "flags": "--endpoint <endpoint>",
          "description": "Teable API server base URL"
        },
        {
          "flags": "--field-id <fieldId>",
          "description": "The ID of the AI field to trigger generation",
          "required": true
        },
        {
          "flags": "--mode <mode>",
          "description": "Fill mode: \"all\" overwrites existing values, \"emptyOnly\" only fills empty cells"
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        },
        {
          "flags": "--record-id <recordId>",
          "description": "Optional: specific record ID. If omitted, fills all matching records"
        },
        {
          "flags": "--table-id <tableId>",
          "description": "The ID of the table containing the AI field",
          "required": true
        },
        {
          "flags": "--token <token>",
          "description": "Personal access token; falls back to TEABLE_TOKEN env var"
        },
        {
          "flags": "--view-id <viewId>",
          "description": "Optional: limit to records visible in this view"
        },
        {
          "flags": "-b, --base-id <baseId>",
          "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
          "required": true
        }
      ],
      "help": "Usage: teable trigger-ai-fill [options]\n\nTrigger AI generation for an AI-configured field.\n\nIMPORTANT: Creating/updating a field with aiConfig does NOT automatically generate content.\nYou MUST call this tool after setting up an AI field to actually run the AI generation.\n\nMODES:\n  - Single cell: provide recordId to fill one specific record\n  - Batch fill: omit recordId to fill multiple records\n\nOPTIONS:\n  - mode: \"all\" (default) overwrites existing values\n  - mode: \"emptyOnly\" only fills cells that are currently empty\n  - viewId: limit to records visible in a specific view\n\nEXAMPLES:\n  1. Fill all records: { tableId, fieldId }\n  2. Fill one record: { tableId, fieldId, recordId: \"recXXX\" }\n  3. Fill empty only: { tableId, fieldId, mode: \"emptyOnly\" }\n\nReturns taskId for background processing. AI generation runs asynchronously.\nSuccess means the background task was accepted, not that generated values are ready.\n\nOptions:\n  --table-id <tableId>    The ID of the table containing the AI field\n  --field-id <fieldId>    The ID of the AI field to trigger generation\n  --record-id <recordId>  Optional: specific record ID. If omitted, fills all matching records\n  --mode <mode>           Fill mode: \"all\" overwrites existing values, \"emptyOnly\" only fills empty\n                          cells\n  --view-id <viewId>      Optional: limit to records visible in this view\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
    },
    {
      "name": "upload-attachment",
      "summary": "Upload a local file as an attachment",
      "description": "Upload local file using direct signed upload flow",
      "options": [
        {
          "flags": "--content-type <mimeType>",
          "description": "Optional MIME type override"
        },
        {
          "flags": "--file-path <path>",
          "description": "Local file path to upload",
          "required": true
        },
        {
          "flags": "--pretty",
          "description": "Indent JSON for human readability",
          "default": false
        }
      ],
      "help": "Usage: teable upload-attachment [options]\n\nUpload local file using direct signed upload flow\n\nOptions:\n  --file-path <path>         Local file path to upload\n  --content-type <mimeType>  Optional MIME type override\n  --pretty                   Indent JSON for human readability (default: false)\n  -h, --help                 display help for command\n"
    },
    {
      "name": "view",
      "description": "view operations (create, get, update, delete)",
      "subcommands": [
        {
          "name": "create",
          "summary": "Create a new view in a table",
          "description": "Create a new view in a table.\n\nParameters:\n  - tableId (required): The table ID\n  - name (required): View name\n  - type (required): View type (grid, kanban, gallery, calendar, form)\n  - description (optional): View description\n\nNote: To add filter, sort, group, etc., use update-view tool after creation.",
          "options": [
            {
              "flags": "--description <description>",
              "description": "Optional description for the view"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--name <name>",
              "description": "Name for the new view",
              "required": true
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to create the view in",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--type <viewType>",
              "description": "View type: grid, kanban, gallery, calendar, form",
              "required": true,
              "choices": [
                "grid",
                "calendar",
                "kanban",
                "form",
                "gallery",
                "plugin"
              ]
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable view create [options]\n\nCreate a new view in a table.\n\nParameters:\n  - tableId (required): The table ID\n  - name (required): View name\n  - type (required): View type (grid, kanban, gallery, calendar, form)\n  - description (optional): View description\n\nNote: To add filter, sort, group, etc., use update-view tool after creation.\n\nOptions:\n  --table-id <tableId>         The ID of the table to create the view in\n  --name <name>                Name for the new view\n  --type <viewType>            View type: grid, kanban, gallery, calendar, form (choices: \"grid\",\n                               \"calendar\", \"kanban\", \"form\", \"gallery\", \"plugin\")\n  --description <description>  Optional description for the view\n  -b, --base-id <baseId>       Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                               base-id <id>`.\n  --token <token>              Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>        Teable API server base URL\n  --pretty                     Indent JSON for human readability (default: false)\n  -h, --help                   display help for command\n"
        },
        {
          "name": "delete",
          "summary": "Delete a view permanently",
          "description": "Delete a view permanently.\n\nParameters:\n  - tableId (required): The table ID\n  - viewId (required): The view ID to delete\n\n⚠️ Warning: This is a permanent operation. Cannot delete the last view in a table.",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table containing the view",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--view-id <viewId>",
              "description": "The ID of the view to delete",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable view delete [options]\n\nDelete a view permanently.\n\nParameters:\n  - tableId (required): The table ID\n  - viewId (required): The view ID to delete\n\n⚠️ Warning: This is a permanent operation. Cannot delete the last view in a table.\n\nOptions:\n  --table-id <tableId>    The ID of the table containing the view\n  --view-id <viewId>      The ID of the view to delete\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "get",
          "summary": "Get all views in a table",
          "description": "Get all views in a table.\n\nParameters:\n  - tableId (required): The table ID\n  - viewId (optional): Filter by specific view ID\n  - viewName (optional): Filter by specific view name\n  - keys (optional): Properties to return (reduces response size)\n\nAvailable view properties:\n  - id: Unique view identifier\n  - name: Display name\n  - type: View type (grid, kanban, gallery, calendar, form)\n  - description: View description\n  - filter: Filter conditions\n  - sort: Sort configuration\n  - group: Group configuration\n  - columnMeta: Column visibility and order (can be large)\n  - shareId: The share id of the view. Use it to access the shared view at `${endpoint}/share/{shareId}/view` (e.g. https://app.teable.ai/share/shrH7kunpHv8U9kfZyD/view).",
          "options": [
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--keys <jsonArray>",
              "description": "Optional: specify which properties to return (reduces response size)"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--table-id <tableId>",
              "description": "The ID of the table to get views from",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--view-id <viewId>",
              "description": "Optional: filter by specific view ID"
            },
            {
              "flags": "--view-name <viewName>",
              "description": "Optional: filter by specific view name"
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable view get [options]\n\nGet all views in a table.\n\nParameters:\n  - tableId (required): The table ID\n  - viewId (optional): Filter by specific view ID\n  - viewName (optional): Filter by specific view name\n  - keys (optional): Properties to return (reduces response size)\n\nAvailable view properties:\n  - id: Unique view identifier\n  - name: Display name\n  - type: View type (grid, kanban, gallery, calendar, form)\n  - description: View description\n  - filter: Filter conditions\n  - sort: Sort configuration\n  - group: Group configuration\n  - columnMeta: Column visibility and order (can be large)\n  - shareId: The share id of the view. Use it to access the shared view at\n`${endpoint}/share/{shareId}/view` (e.g. https://app.teable.ai/share/shrH7kunpHv8U9kfZyD/view).\n\nOptions:\n  --table-id <tableId>    The ID of the table to get views from\n  --view-id <viewId>      Optional: filter by specific view ID\n  --view-name <viewName>  Optional: filter by specific view name\n  --keys <jsonArray>      Optional: specify which properties to return (reduces response size)\n  -b, --base-id <baseId>  Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                          base-id <id>`.\n  --token <token>         Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>   Teable API server base URL\n  --pretty                Indent JSON for human readability (default: false)\n  -h, --help              display help for command\n"
        },
        {
          "name": "update",
          "summary": "Update view configuration",
          "description": "Update any view property: name, description, filter, sort, group, columnMeta, options, or locked status.\n\nAt least one property must be provided. For complex properties (filter/sort/group/columnMeta), use get-doc tool:\n  • get-doc view.filter - Filter conditions\n  • get-doc view.sort - Sort configuration\n  • get-doc view.group - Group configuration\n  • get-doc view.column - Column metadata\n\nReturns: {success, viewId, updated}",
          "options": [
            {
              "flags": "--column-meta <jsonArray>",
              "description": "Column metadata (see get-doc view.column)"
            },
            {
              "flags": "--description <description>",
              "description": "New view description"
            },
            {
              "flags": "--endpoint <endpoint>",
              "description": "Teable API server base URL"
            },
            {
              "flags": "--filter <jsonObject>",
              "description": "View filter conditions (see get-doc view.filter)"
            },
            {
              "flags": "--group <jsonArray>",
              "description": "View group configuration (see get-doc view.group)"
            },
            {
              "flags": "--is-locked",
              "description": "Lock view to prevent edits"
            },
            {
              "flags": "--name <name>",
              "description": "New view name"
            },
            {
              "flags": "--options <jsonObject>",
              "description": "View-specific options (grid/kanban/gallery/calendar/form)"
            },
            {
              "flags": "--pretty",
              "description": "Indent JSON for human readability",
              "default": false
            },
            {
              "flags": "--sort <jsonObject>",
              "description": "View sort configuration (see get-doc view.sort)"
            },
            {
              "flags": "--table-id <tableId>",
              "description": "Table ID containing the view",
              "required": true
            },
            {
              "flags": "--token <token>",
              "description": "Personal access token; falls back to TEABLE_TOKEN env var"
            },
            {
              "flags": "--view-id <viewId>",
              "description": "View ID to update",
              "required": true
            },
            {
              "flags": "-b, --base-id <baseId>",
              "description": "Target base ID (required, e.g. bseXXXX). Set default: `teable config base-id <id>`.",
              "required": true
            }
          ],
          "help": "Usage: teable view update [options]\n\nUpdate any view property: name, description, filter, sort, group, columnMeta, options, or locked\nstatus.\n\nAt least one property must be provided. For complex properties (filter/sort/group/columnMeta), use\nget-doc tool:\n  • get-doc view.filter - Filter conditions\n  • get-doc view.sort - Sort configuration\n  • get-doc view.group - Group configuration\n  • get-doc view.column - Column metadata\n\nReturns: {success, viewId, updated}\n\nOptions:\n  --table-id <tableId>         Table ID containing the view\n  --view-id <viewId>           View ID to update\n  --name <name>                New view name\n  --description <description>  New view description\n  --filter <jsonObject>        View filter conditions (see get-doc view.filter)\n  --sort <jsonObject>          View sort configuration (see get-doc view.sort)\n  --group <jsonArray>          View group configuration (see get-doc view.group)\n  --column-meta <jsonArray>    Column metadata (see get-doc view.column)\n  --options <jsonObject>       View-specific options (grid/kanban/gallery/calendar/form)\n  --is-locked                  Lock view to prevent edits\n  -b, --base-id <baseId>       Target base ID (required, e.g. bseXXXX). Set default: `teable config\n                               base-id <id>`.\n  --token <token>              Personal access token; falls back to TEABLE_TOKEN env var\n  --endpoint <endpoint>        Teable API server base URL\n  --pretty                     Indent JSON for human readability (default: false)\n  -h, --help                   display help for command\n"
        }
      ],
      "help": "Usage: teable view [options] [command]\n\nview operations (create, get, update, delete)\n\nOptions:\n  -h, --help        display help for command\n\nCommands:\n  create [options]  Create a new view in a table\n  get [options]     Get all views in a table\n  update [options]  Update view configuration\n  delete [options]  Delete a view permanently\n  help [command]    display help for command\n\nExamples:\n  teable view get --table-id tblXXXX\n  teable view create --table-id tblXXXX --name \"My Grid\" --type grid\n"
    }
  ]
}
